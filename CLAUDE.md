# CLAUDE.md - zalo-agent

Bot AI thường trú trên Zalo **tài khoản cá nhân**, nhiều account chạy chung một process.
Luồng: tin nhắn Zalo -> lọc -> gộp theo thread -> agent loop (LLM tự gọi tool) -> trả lời.

Chi tiết kiến trúc + lý do từng quyết định kỹ thuật: `docs/system-architecture.md`.
Trạng thái V1/V2/V3 + việc còn treo: `docs/project-roadmap.md`.

## Repo tham khảo (chỉ đọc, KHÔNG sửa)

Nằm ngoài project tại `D:\source-code\zalo-agent-references\`:

| Repo | Dùng để tra |
|---|---|
| `zca-js` | Source lib chính - chữ ký hàm, types, `examples/`, `src/apis/` |
| `zalo-agent-cli` | Pattern QR login, lưu credential mã hóa |
| `zalo-personal` | Cách gọi zca-js cho từng tính năng (react, file, tag, group) |
| `deplao-builder` | Pattern quản lý nhiều instance zca-js cùng lúc |
| `goclaw` | Pattern tool hệ thống (datetime, web search chain, UI Built-in Tools). **CC BY-NC 4.0: chỉ soi pattern, KHÔNG copy code** |
| `hermes-agent` | Toolset theo kênh, webhook-safe toolset, cascade web search (MIT - dùng thoải mái) |
| `9router` | Source của chính router đang dùng (MIT). Tra `open-sse/executors/` + `open-sse/translator/` khi cần biết router làm gì với request: khóa cache phiên, `cache_control`, `reasoning_effort`, cloaking |
| `deepseek-harness` | Coding-agent harness chuẩn doanh nghiệp (MIT, DeepSeek). ~85% KHÔNG áp được cho chat bot phản ứng (event-sourced/plugin-DI/Cordis) - chỉ soi META-RULE, KHÔNG bê machinery. Đã nhặt: timeout-policy, compaction prompt cấu trúc, renderReminderFraming, per-step token meter (xem `plans/260824-0116-ap-dung-pattern-tu-deepseek-harness/`) |

**Trước khi viết code đụng zca-js, hãy grep trong `zalo-agent-references\zca-js\src\apis\` để xác nhận chữ ký hàm thật thay vì đoán.** Đã có tiền lệ đoán sai: `loginQR` KHÔNG tự ghi file QR khi mình truyền callback - phải tự gọi `event.actions.saveToFile()`.

## Ràng buộc bắt buộc

- **Chỉ dùng nick phụ.** zca-js là API không chính thức, có thể bị Zalo khóa tài khoản.
- **Không nối tool chuyển tiền/thanh toán vào agent.** Bot đọc tin từ người lạ; một tin soạn khéo (prompt injection) có thể lừa agent gọi tool. `zalo-agent-cli` có sẵn bank transfer/VietQR - không harvest phần đó.
- **Không commit `data/`, `.env`, `config/accounts.json`** (đã gitignore). `data/` chứa cookie Zalo mã hóa + SQLite history.
- **1 listener/account**: mở Zalo Web trên trình duyệt sẽ đá listener của bot (bot tự reconnect và đá ngược lại).
- **Không làm gửi tin hàng loạt / campaign CRM** - user đã chủ động loại khỏi phạm vi.

## Quyết định đã chốt (đừng lật lại nếu không có bằng chứng mới)

- Dùng `zca-js` qua npm, **không fork**. Update lib bằng `pnpm update`.
- Agent loop **tự viết** bằng Vercel AI SDK, không dùng framework agent (OpenClaw/Hermes/Zylos).
- LLM gọi qua **router proxy 9Router** (`LLM_PROVIDER=openai-compatible`) để đổi provider bằng env, không sửa code.
- History dùng **`node:sqlite` built-in**, không dùng better-sqlite3 (cần Visual Studio Build Tools trên Windows).
- tsconfig để `moduleResolution: Bundler` - `index.d.ts` của zca-js dùng directory import, NodeNext không resolve được types.
- Tạo file Office bằng `docx` + `exceljs` (npm), **không** dùng `write-excel-file` dù nhẹ hơn 12 lần: nó không ghi được giá trị cache `<v>` cho công thức nên mọi công cụ xem trước hiện ô trống, và ghi thừa dấu bằng trong `<f>` (sai ECMA-376). Cũng **không** cài LibreOffice để recalc - bot tự tính kết quả rồi ghi kèm.
- Tool tạo file chỉ nhận **dữ liệu** (tiêu đề, đoạn văn, bảng), tuyệt đối không chạy code do model sinh ra: bot đọc tin người lạ nên đó là đường prompt injection thành RCE.
- **Tool KHÔNG ném lỗi ra agent loop** - mọi nhánh hỏng bắt lại rồi trả câu cho model diễn giải. Hệ quả dễ quên: AI SDK chỉ sinh phần `tool-error` khi `execute` NÉM, nên nhánh hỏng trả chuỗi trơn là vô hình với `tool-loop-guard`. Vì vậy **mọi nhánh hỏng PHẢI bọc bằng `ketQuaLoi(...)`** (`src/agent/tools/tool-failure-result.ts`) - object `{ok:false,loi}` chứ không phải tiền tố chuỗi, vì nội dung web/tin nhắn của người lạ đi thẳng vào chuỗi kết quả nên luật dựa trên chữ có thể bị soạn cho trùng. Nhánh thành công vẫn trả chuỗi trần. Test dùng `loiCuaTool()` để khẳng định luôn cả dấu hiệu.
- Vẽ ảnh gọi `/v1/images/generations` với **`?response_format=binary` + JPEG** (157 KB so với 2.4 MB của JSON base64 PNG). Ảnh gốc để sửa nằm ở trường **`image`** dạng data URI, KHÔNG phải `ref_image` - sai tên thì provider bỏ qua âm thầm và ra ảnh vẽ mới. **Không gửi `size`**: model bỏ qua tham số này (đo: xin 1024x1024 nhận 1536x1024). Lỗi có 2 hình dạng - `400` trả `error.message`, `401` trả `error` là chuỗi.
- 12-Factor #7 ("Contact Humans with Tool Calls") hiểu là **agent chủ động HỎI LẠI khi thiếu thông tin**, KHÔNG phải approval gate xin phép trước khi chạy tool. Bản audit đầu đặt sai bài toán này. Approval gate ở đây gần như vô giá trị: 6 tool tác động ra ngoài đã bị loại khỏi lượt theo lịch, nội dung web đã bọc `<noi_dung_ngoai>`, người đang chat thường là chủ bot - hỏi "gửi file nhé?" sau một lệnh rõ ràng chỉ là ma sát. Luật nằm ở 3 dòng cuối khối "Quy tắc trả lời" của `BASE_PERSONA` (khi nào hỏi / khi nào KHÔNG hỏi / hỏi thế nào), không thêm tool `ask_user`, không thêm bảng chờ. **Đã đo: bỏ cả 3 dòng thì eval vẫn xanh** - model mạnh tự biết hỏi; giữ lại vì rẻ và vì ghi rõ ý định cho lần đổi model, không phải vì đã chứng minh tác dụng.
- Lịch hẹn (`src/scheduler/`, tool `schedule_task`): KHÔNG nhận chuỗi datetime, chỉ `{date,time}` rời hoặc `{inMinutes}` - chuỗi naive qua `new Date()` bị hiểu theo giờ hệ điều hành, đúng lỗi Hermes từng dính (`jobs.py`, issue #51021). Quy đổi giờ CHỈ đi qua `zonedWallClockToUtc` (luxon, dependency trực tiếp trong `package.json` dù `cron-parser` đã kéo gián tiếp - pnpm layout chặn import gián tiếp).
- **`schedule_task` action `create` KHỬ TRÙNG theo `(thread, kind, lịch, TÊN)` - cố ý KHÔNG có `payload`.** Ca thật 2026-08-20 đọc từ trace: người dùng nhắn "okay cảm ơn bạn" sau khi bot đã đặt lịch xong, bot TẠO LẠI y hệt. Model tự khai ý định: "Mình KIỂM TRA và chốt lịch nhắc ngay" - nó muốn kiểm tra, nhưng `historyToModelMessages` chỉ trả `{role:"assistant", content}` nên sang lượt sau model KHÔNG thấy tool call của lượt trước, không có bằng chứng nào là lịch đã đặt; thứ duy nhất trong tầm với để "kiểm tra" là `create`. Số đo từ hai job đó: `name`/`kind`/`schedule` trùng khít, `payload` KHÁC (model viết lại câu) - nên khóa nào có `payload` là trượt đúng ca cần bắt. Giữ `name` trong khóa vì bỏ nó đi là chặn oan ca THẬT "hẹn hai việc khác nhau vào cùng một giờ" (người dùng chốt). Đổi lại phải chấp nhận đây là phỏng đoán theo tên; lưới đỡ là câu "LƯU Ý đã có lịch khác cùng mốc giờ" ghép vào kết quả - không chặn, chỉ đưa thông tin model đang thiếu. Khử trùng chạy TRƯỚC `checkThreadJobCap`: lời gọi trùng không thêm job nào nên báo "chạm trần" là báo sai chuyện.
- Job `once` trễ quá `SCHEDULER_ONCE_GRACE_MINUTES` vẫn GỬI kèm nhãn "(nhắc trễ, lịch gốc HH:MM)", không im lặng nuốt như Hermes/goclaw - bot cá nhân thì mất tích một lời đã hẹn là kết cục tệ nhất. Không retry ở tầng scheduler (agent-loop đã có 2 tầng chống chịu, thêm nữa là chồng 3 tầng); riêng lỗi ĐƯỜNG GỬI thử lại tối đa 3 lần qua cột bền `delivery_attempts`.
- Lượt agent theo lịch chạy `isolated: true`, loại 10 tool khỏi lượt này (`runsInScheduledTurn: false` trong `tool-registry.ts`): 4 tool thiếu hạ tầng cho lượt cô lập + 6 tool (`send_file`, `create_word_document`, `create_excel_file`, `create_image`, `tai_video`, `tag_member`) gọi thẳng `enqueueSend` né hoàn toàn trần `SCHEDULER_MAX_PROACTIVE_PER_DAY`. Trần đó đếm theo TIN ZALO thật (không phải lượt job) ở bảng bền `proactive_send_counters` (không đếm bộ nhớ - mất khi restart; không đếm từ `scheduled_job_runs` - bị prune).
- **Scheduler gửi qua `KenhLuot`, KHÔNG qua `api` zca-js.** Mọi `ReplyTarget` của job dựng bằng `taoDichGuiChoJob` (`scheduled-job-reply-target.ts`), chính nó gọi `replyTargetTuKenh` - một chỗ duy nhất biết trường nào của kênh phải chở theo (`tranKyTuMotTin`, `mangDinhDang`; cả hai TÙY CHỌN nên quên là trình biên dịch im lặng còn hậu quả thì câm). `getRunningAccountApi` đã bị XÓA: mọi caller của nó đều đi tiếp một bước giống hệt nhau (tự dựng `duongGuiZcaJs`), tức nó là cái bẫy có hình dạng tiện lợi - đó chính là lý do lịch hẹn không chạy được trên kênh bot suốt từ V3.16. Cần `api` cho tool thì `getRunningAccountKenh(id)?.api`. Nhớ luôn: đường gửi THÔNG BÁO CHẠM TRẦN NGÀY (`scheduled-job-cap-guard.ts`) là một chỗ RIÊNG, có ca test riêng - sửa mỗi đường gửi chính là để lại một lỗ hỏng câm.
- **Kênh khai `mangDinhDang: false` thì `styles` bị vứt ở `sendReplyInParts`, TRƯỚC bộ cắt.** `soByteTin` cộng cả `JSON.stringify({styles})` vào ngân sách byte mà `kenhBot.duongGui` thì vứt chúng - giữ lại là tính tiền cho thứ không đi trên dây rồi chẻ thừa tin. Chữ VẪN qua `dinhDangNeuBat` để bóc dấu markdown; bỏ hẳn bước đó là đẩy `**` thô xuống Zalo. Hệ quả phụ đáng nhớ: `laLoiMayChuTuChoi` đọc `err.code` dạng SỐ của `ZaloApiError` nên không nhận ra `LoiZaloBotApi` - hôm nay vô hại (kênh bot hết `styles` để mà bỏ) nhưng ai "dọn dẹp" hàm đó sẽ bật ra một lời gọi API thừa mỗi lần server từ chối.
- "Chạy thử ngay" trên dashboard phải GIÀNH job (`next_run_at = NULL`) trước khi dispatch, giống clear-before-dispatch của vòng tick thật - không thì tick thật xen vào giữa gây gửi trùng.
- **MỌI lời gọi LLM đi qua `chayStream()` (`stream-text-result.ts`), không còn `generateText` ở đâu cả.** Router nằm sau Cloudflare, mà Cloudflare cắt bằng 524 khi origin chưa trả BYTE ĐẦU trong 100 giây; request non-stream buộc router gom trọn câu trả lời rồi mới gửi. Đo trên router thật với prompt sinh 4000 chữ: `stream=false` chết 524 ở giây 125, `stream=true` xong ở giây 135 với byte đầu ở giây 7,5 - streaming chạy LÂU HƠN mốc bị cắt mà vẫn qua. Bot vẫn KHÔNG stream chữ xuống Zalo. `streamText` có 3 hành vi phải vá và cả 3 đều hỏng CÂM: `onError` mặc định `console.error` thô làm rò `requestBodyValues` ra stdout; lỗi gốc bị thay bằng `NoOutputGeneratedError` lúc flush (mất mã HTTP = cả tầng chữa lỗi provider thành mù); lỗi sau step 1 thì promise VẪN resolve với kết quả cụt. Bộ bắt lỗi phải dựng MỚI mỗi lần gọi.
- Bộ gộp tin chốt batch khi CẢ HAI đúng: im lặng đủ `debounceMs` VÀ không còn ai giữ khoá thread. Bận thì ĐỖ LẠI trong `pending` chứ không xếp hàng - xếp hàng là chốt cứng danh sách nên tin tới sau không còn chỗ gộp và đẻ ra lượt mới (đúng lỗi ngày 2026-08-04: lượt 249 giây, 3 tin thành 3 lượt, mỗi lượt làm lại từ đầu). Điều kiện (b) nói về KHOÁ, không phải mọi lượt agent - lượt theo lịch cố ý chạy ngoài khoá để tránh deadlock.
- Tin nhắn thêm giữa lượt được chèn ở ranh giới step qua `prepareStep`. **Tin chen PHẢI được giữ ở tầng `agent-loop` (`tinChenDaKeo`), không chỉ trong mảng nội bộ của SDK**: `layTinDangDo` đã xóa tin khỏi hàng chờ, mà mọi nhánh chữa lỗi và LƯỢT CHỐT đều dựng lại từ biến `messages` của agent-loop. Bỏ mảng đó thì đúng lượt dài - lượt duy nhất đủ lâu để người ta kịp nhắn thêm - lại là lượt đánh rơi tin chen, mà lượt chốt mới là nơi sinh ra câu gửi xuống Zalo. Trong lượt chốt tin chen nằm ở đuôi ĐƯỢC BẢO VỆ cùng câu hỏi và lời nhắc.
- **Sổ sách trần ngày của scheduler dùng CHUNG một mốc thời gian cho cả lượt** (`RunScheduledJobOptions.now`, chốt ở đầu tick). Mọi hàm trong `proactive-send-guard.ts` KHÔNG có `= new Date()` - cố ý, để trình biên dịch canh. Bug đã trả giá: `recordProactiveSend` mặc định giờ thật + `sendCapNotice` quên truyền `now` -> vừa cộng đếm nhầm ngày vừa dọn bảng xóa mất chính dòng vừa ghi. Nặng nhất là ca nửa đêm: giành suất ngày D, gửi mất 20s rồi hỏng, hoàn suất trả cho ngày D+1 - ngày D giữ suất vĩnh viễn. Đã cân nhắc hướng ngược (dùng giờ thật khắp nơi) và BÁC vì giành/hoàn cách nhau cả quãng gửi.
- **Tin người dùng ghi vào lịch sử NGAY LÚC NHẬN** (`record-incoming-message.ts`, gọi từ router TRƯỚC `enqueueMessage`), không phải ở cuối lượt như bản trước. Lý do: thứ tự trong DB phải là thứ tự tin TỚI, không phải thứ tự lượt kết thúc - điều kiện cần cho lượt chạy song song; và lượt chết giữa chừng không đánh rơi tin. Hệ quả phải nhớ: lúc lượt đọc lịch sử thì tin của CHÍNH nó đã nằm trong đó, nên `buildTurnMessages` lọc theo `historyRowId`, và agent-loop phải đọc DƯ từ DB đúng bằng số dòng sắp bị lọc - không thì cửa sổ `HISTORY_CONTEXT_LIMIT` teo lại theo cỡ batch mà không ai thấy (batch chạm trần 32 tin là model thấy 0 tin cũ). Tin đang CHỜ trong hàng gộp cũng phải loại (`idTinDangCho`): nó sẽ được `layTinChen` chèn lại kèm nhãn, không loại là model đọc hai lần.
- 6 tool gửi thẳng qua `enqueueSend` (`send_file`, 2 tool tài liệu, `create_image`, `tai_video`, `tag_member`) phải **ghi nhận qua `ghiNhanDaGui`, KHÔNG tự gọi `appendMessage`**: chỉ `message-turn-processor` biết đủ ngữ cảnh lượt. Nó ghi ngay lúc được gọi. Thiếu đường này thì tin bot gửi mất khỏi cả dashboard lẫn trí nhớ của chính bot ở lượt sau.
- **KHÔNG tách `agent-loop.ts`** dù nó 657 dòng: ứng viên duy nhất là `runWrapUp`, mà nó ôm 12 biến closure - tách ra là một tham số object to đùng, rủi ro cao mà không thêm tính đúng đắn nào. Đã cân nhắc và chốt giữ nguyên.
- Câu "trấn an khi bắt chờ" (`BUSY_ACK_AFTER_MS`) mặc định 600s chứ KHÔNG báo bận mọi lúc: người nhắn đã có ba tín hiệu (dấu "đang nhập", "đã xem", auto-react - tin chen cũng nhận đủ cả ba). Khe hở thật là typing tự tắt ở phút 10 còn lượt chạy tới phút 15.
- Kho tri thức (`src/knowledge/`, tool `kb_search`): nội dung nạp bằng TOOL, KHÔNG tự nhét vào prompt - giữ nguyên khoản đầu tư prompt cache của những lượt không đụng KB. Mặc định agent KHÔNG đọc được nguồn nào (`agent_kb_sources` rỗng nghĩa là đóng, không phải mở hết) - phải gán tường minh ở trang sửa agent.
- Tải video (`src/video/`, tool `tai_video`): **TikWM là nguồn CHÍNH cho TikTok, yt-dlp là DỰ PHÒNG; Facebook chỉ có yt-dlp.** Đo cùng IP cùng khung giờ: TikWM 12/12 trong 1,04s trả h264, yt-dlp 3/7 trong 3,94s trả h265 (TikTok trả trang thử thách chống bot; đã thử `--impersonate`, `api_hostname`, `device_id`, thử lại - không cái nào kéo nổi tỉ lệ). Hai tầng ĐỘC LẬP THẬT (một cái gọi API bên thứ ba, một cái tự cào trang) - dự phòng mà cả hai cùng dựa vào một cơ chế thì không phải dự phòng. TikWM trả `Url parsing is failed` với Facebook.
- **Lấy `data.play` của TikWM, TUYỆT ĐỐI không `data.wmplay`, và thiếu `play` thì HỎNG chứ không lùi về `wmplay`.** Đã kiểm bằng MẮT trên hai file tải thật: `wmplay` có logo TikTok DI CHUYỂN theo thời gian, `play` sạch, lệch 833.225 byte. Lấy nhầm trường không làm gì đỏ - bot vẫn gửi được, vẫn báo thành công, chỉ là video bẩn. Video ĐĂNG LẠI thì watermark nằm sẵn trong file gốc, không API nào gỡ được (ca này từng làm tưởng TikWM cũng bẩn - URL `@tiktok` nhưng watermark ghi `@gorilloyt`).
- **KHÔNG gọi `sendVideo` rồi trông vào `catch` để biết URL hỏng - nó KHÔNG ném.** Đọc zca-js `sendVideo.ts:69-77`: `if (headResponse.ok)`, tức 403/404 thì `fileSize` giữ 0 và nó VẪN POST tin chứa URL chết. Hệ quả đã trả giá: nhánh dự phòng thành code chết, bot trừ suất, ghi lịch sử "đã gửi video", báo model thành công - còn người nhận thấy một thẻ video không mở được. Hôm nay `sendVideo` luôn nhận URL của chính Zalo nên bẫy đó không còn với chỗ gọi này, nhưng bộ dò (`kiemUrlVideoConSong`) vẫn phải chạy TRƯỚC vì nó quyết định lấy byte ở đâu. Cùng họ: `mediaType` của bản tải về phải kiểm lại, vì `200 + text/html` (trang "link hết hạn") cũng không ném.
- **Dò bằng `GET` kèm `Range: bytes=0-0`, TUYỆT ĐỐI không bằng HEAD.** Đo thật: cùng một video, `v16m.tiktokcdn-us.com` trả **503 cho HEAD** nhưng **206 cho GET có Range**, còn `v19.tiktokcdn-us.com` trả lời HEAD bình thường. HEAD hỏng KHÔNG có nghĩa video hỏng - máy người nhận tải bằng GET và vẫn xem được. Dò bằng HEAD đẩy oan video sống sang đường đắt nhất ở **3/6** lượt; đổi sang Range thì **6/6**. Với 206 thì cỡ thật nằm ở đuôi `content-range`, KHÔNG phải `content-length` (cái đó là 1 byte).
- **MỘT đường gửi, và byte BUỘC phải đi qua bot.** Rà cả 140 API của zca-js: không có đường nào đưa Zalo một URL rồi Zalo tự tải về tự host. Đo bằng lượt gửi thật tới điện thoại người dùng, cùng một video chỉ đổi chỗ chứa: `videoUrl` trỏ CDN TikTok/Facebook thì MÁY TÍNH xem được còn ĐIỆN THOẠI KHÔNG PHÁT ĐƯỢC; `videoUrl` trỏ hạ tầng Zalo thì điện thoại xem mượt. Nên luồng là: dò URL -> lấy byte vào RAM (dò qua thì tải thẳng, dò trượt thì để **yt-dlp TỰ TẢI từ URL GỐC**) -> đọc khung hình từ chính buffer -> dựng poster (xem dòng dưới) -> `uploadAttachment` -> `sendVideo`; poster dựng KHÔNG được thì **gửi DẠNG FILE** (`sendMessage` với attachment `.mp4` từ Buffer - không cần thumbnail, vẫn không chạm đĩa). Vì sao hai đường lấy byte không gộp được: URL của yt-dlp gắn với phiên của nó, đo được trả **403 ngay trên chính máy vừa chạy yt-dlp**, nên tự tải URL đó cũng trượt; để nó tự tải thì được. `--max-filesize` chặn TRƯỚC khi tải, nhưng chỉ khi nguồn có khai cỡ - lưới đỡ thật là phép kiểm độ dài buffer.
- **HAI cách gửi đều CHẠY ĐƯỢC trên máy thật, đừng đo lại**: `sendVideo` (thẻ video, ĐANG DÙNG) và gửi dạng FILE `.mp4` - cả hai tốt trên cả máy tính lẫn điện thoại, đã test cùng một video tới điện thoại người dùng. Chọn thẻ video vì đẹp hơn, KHÔNG phải vì cách kia hỏng, nên gửi dạng file là đường lui đã kiểm chứng nếu `uploadAttachment` gặp vấn đề. Thứ DUY NHẤT bị loại là `videoUrl` trỏ CDN ngoài: nó gửi được, ảnh bìa đúng, không crash, máy tính xem tốt - chỉ ĐIỆN THOẠI không phát được. Đừng đo tính năng video trên máy tính rồi kết luận.
- **KHUNG HÌNH phải đọc từ CHÍNH FILE (`doc-khung-hinh-mp4.ts`), không tin nguồn khai.** Khai sai làm ứng dụng Zalo trên ĐIỆN THOẠI crash - nó dựng sẵn bề mặt phát theo số ta khai; máy tính co giãn được nên không ai thấy. Không nguồn nào khai đúng: TikWM KHÔNG có width/height (đã dump toàn bộ khóa), yt-dlp format `hd` không mang ở đâu, mảng `formats` khai 2560x1440 trong khi ffprobe trên luồng gửi đi cho 1280x720. Offset trong `tkhd` là ĐO THẬT rồi đối chiếu ffprobe, đừng tính nhẩm: bản đầu lệch 4 byte và đọc ra chiều cao 16384 (phần tử cuối của ma trận biến đổi). Bẫy thứ hai: `tkhd` ĐẦU TIÊN của file TikTok là track ÂM THANH, khai 0x0 - phải bỏ qua rồi đi tiếp. Ma trận `a===0 && b!==0` nghĩa là xoay 90/270 độ, phải đổi chiều.
- **THỜI LƯỢNG cũng đọc từ file (`mvhd`) nhưng NGUỒN TRƯỚC, file lấp chỗ trống** - ngược với khung hình (file LUÔN thắng). Lý do bất đối xứng: khai sai khung -> CRASH app, nên phải tin file; khai sai thời lượng chỉ là nhãn hiển thị nên tin nguồn (TikTok/FB trả giây tin cậy) trước, file chỉ lấp khi nguồn im (Instagram trả `duration: null` -> `durationMs=0`). `docThongTinMp4` đọc CẢ `tkhd` lẫn `mvhd` trong MỘT lượt duyệt (đừng tách hai lượt); `docKhungHinhMp4` là wrapper trả `.khung` cho chỗ chỉ cần kích thước. Offset mvhd (v0 timescale@12/duration@16; v1 @20/@24) ĐO THẬT khớp ffprobe, có trần 24h chặn sentinel `0xFFFFFFFF` ("không biết"). Đừng đổi policy thành "file luôn thắng" cho thời lượng - nguồn tin cậy hơn cho ca thường.
- **Poster = tải ảnh bìa NGUỒN rồi UPLOAD lên Zalo (`chuan-bi-anh-bia-video.ts`), KHÔNG dùng `parseLink`.** Ba đường đặt poster đã đo thật tới điện thoại: `thumbnailUrl` trỏ host ngoài -> thẻ ĐEN THUI; `thumbnailUrl` rỗng -> Zalo TỪ CHỐI (`ZaloApiError code 114`); `parseLink` -> với Facebook trả một ảnh PLACEHOLDER rác ("feed_thumb_link" 2019) trên `zadn.vn` - nó LOAD được nên `anhBiaZalo:true` là niềm tin giả, "kiểm ảnh có load" cũng không bắt được. Đường đúng: `downloadFromPublicUrl` ảnh bìa nguồn vào RAM (gác SSRF, trần 5MB) -> `readImageSize` đọc kích thước bằng BYTE (không decoder) -> `uploadAttachment` nhánh IMAGE (đồng bộ, KHÔNG cần listener, khác video) -> dùng `normalUrl`. `UploadAttachmentVideoResponse` KHÔNG có `thumbUrl` nên Zalo không dựng poster giúp cho video - phải tự cấp. Dựng không được (không có ảnh nguồn/tải hỏng/WebP đọc không ra cỡ/upload lỗi) thì trả `null` -> **gửi dạng file**, KHÔNG placeholder (người dùng chốt: thà file còn hơn nhìn rẻ). KHÔNG trích khung tại chỗ: Node không có WebCodecs, "WebCodecs cho Node" trên npm đều là ffmpeg đội lốt, decoder thuần JS chỉ Baseline (TikTok/FB dùng Main/High) và bỏ hoang - đúng mặt tấn công thiết kế này tránh.
- **`uploadAttachment` cho video CẦN listener đang chạy, và zca-js KHÔNG đặt timeout.** Nó đăng ký callback theo `fileId`, chỉ giải quyết khi sự kiện hoàn tất tới qua WEBSOCKET (`listen.ts`). Mất listener là promise treo VĨNH VIỄN - đã gặp thật lúc thử, bot đang chạy giữ mất suất listener nên lời gọi treo tới khi bị giết. Vì vậy `upLenZalo` bọc `Promise.race` với trần 5 phút. Lưu ý đây là ngoại lệ có lý do cho luật "KHÔNG bọc `Promise.race` quanh việc đã xếp `enqueueSend`" ở trên: chỗ kia race quanh một việc ĐÃ VÀO hàng đợi (bỏ cuộc không hủy được nó, và bên kia trả lời muộn thì gửi hai lần); chỗ này race quanh một promise treo mà bỏ nó đi thì không có gì được gửi cả.
- **KHÔNG bọc `Promise.race` quanh việc đã xếp `enqueueSend`.** Đo thật: việc bị race bỏ VẪN là đuôi hàng đợi của thread nên việc kế tiếp phải chờ nó xong - trần không cứu được gì, mà nếu bên kia trả lời sau khi race bỏ cuộc thì hai đường cùng gửi, người dùng nhận **hai video**. Cộng rò rỉ đo được: `setTimeout` không `clearTimeout` giữ tiến trình sống thêm 60 giây sau khi việc đã xong. Bước dò thay thế nó.
- **yt-dlp nhận DANH SÁCH CHO PHÉP biến môi trường, không nhận `process.env`** - và MỌI đường chạy yt-dlp đi qua `chay-yt-dlp.ts`, một chỗ duy nhất giữ ba lớp siết (`envToiThieu`, `--ignore-config`, `--no-plugin-dirs`). Nó phân tích nội dung của một URL do người lạ gửi nên không có việc gì nhìn thấy `CREDENTIALS_ENCRYPTION_KEY` hay khóa API router; danh sách CHO PHÉP để biến bí mật thêm sau này tự động nằm ngoài. Hai cờ kia chặn config file (`--exec` = chạy lệnh tùy ý) và plugin (mã Python chạy trong chính tiến trình). Bẫy đã trả giá: bỏ `APPDATA` thì Python bỏ luôn user site-packages, bản `pip install --user` biến mất và báo `No module named yt_dlp` - y hệt ca chưa cài.
- **Thiếu công cụ có HAI hình dạng, và ca Docker là hình dạng thứ hai.** `ENOENT` chỉ bắn khi thiếu chính binary; ca thật của Docker (pip install hỏng, đổi base image) là thoát mã **1** kèm stderr `No module named yt_dlp` - ĐO THẬT, `err.code === 1` chứ không phải `"ENOENT"`. Bắt mỗi ENOENT là bỏ lọt đúng ca cần bắt nhất và model lại nhận câu "video có thể ở chế độ riêng tư". Chở lên tool bằng **cờ có kiểu `loiCauHinh`**, không bằng chữ trong `loi` (`loi` chở stderr của bên thứ ba nên đã đổi tên thành `loiChoLog`).
- **KHUNG HÌNH khai sai làm CRASH ứng dụng Zalo trên điện thoại** - ca thật, người dùng gặp. Video Facebook 1280x720 NGANG: yt-dlp chọn format `hd`, mà format đó không mang `width`/`height` ở đâu cả, nên bản cũ lùi về mặc định DỌC 576x1024. Zalo được bảo "video dọc" rồi nhận khung ngang: máy tính co giãn được, ứng dụng điện thoại dựng sẵn bề mặt phát theo số đã khai rồi CRASH; người dùng thấy một thẻ đen khung dọc. Kích thước thật CÓ trong mảng `formats` (`khungHinh` đọc ở đó), chỉ là bản cũ không đọc tới. Thứ tự: cấp trên -> format lớn nhất trong `formats` (giữ TỈ LỆ, chuẩn hóa cạnh dài về 1280 vì độ phân giải thật của luồng `hd` thì không cách nào biết - đo được formats khai 2560x1440 trong khi ffprobe trên luồng gửi đi cho 1280x720) -> mặc định THEO NỀN TẢNG (TikTok dọc, Facebook ngang). ĐỪNG dùng một hằng số mặc định chung cho mọi nền tảng.
- **Story Facebook KHÔNG tải được, và đó không phải lỗi của mình.** Hai chặn độc lập, đo bằng phép đối chứng cùng công cụ: (a) máy chủ Facebook trả `302 -> login.php?next=<url>` - người dùng mở được vì TRÌNH DUYỆT của họ gửi kèm cookie phiên, quyền xem nằm ở cookie chứ không nằm trong URL, còn máy chủ bot không có phiên nào; (b) rà cả 1751 extractor của yt-dlp thì `/stories/<id>/<mã>/` không khớp cái nào, phải rơi xuống `generic` (`story.php`/`story_fbid` trong pattern Facebook là BÀI ĐĂNG kiểu cũ, không phải Stories 24 giờ). Đối chứng: `share/v/` cũng rơi xuống generic nhưng được chuyển hướng tới `/reel/...` rồi `facebook:reel` nhận - nên nó chạy. KHÔNG nhét cookie Facebook lên VPS để chữa. Chở lên tool bằng cờ `canDangNhap` để bot nói đúng bệnh thay vì "có thể video ở chế độ riêng tư" - nói sai là người dùng thử lại vô ích.
- **Câu lỗi tải video tách TẠM THỜI (thử lại được) khỏi VĨNH VIỄN (riêng tư/đã xóa) - ĐỪNG gộp lại một câu.** Chuỗi nguồn chở cờ `tamThoi` (`chuoi-nguon-video.ts`), bật khi CÓ nguồn hỏng `thuLaiDuoc:true` (OR tích lũy qua cả chuỗi, KHÔNG last-wins: TikWM 5xx TRƯỚC rồi yt-dlp "Private" SAU vẫn phải `true`). Ca thật đã trả giá: short link app và full link desktop cùng trỏ MỘT video khóa vùng + yt-dlp chống bot; câu generic gộp cũ khiến model bịa "gửi lại link đầy đủ" - dắt người dùng đi vòng (đổi dạng link vô ích, whitelist đã nhận `vt.tiktok.com` và yt-dlp tự resolve short link). Hai câu: tạm thời khuyên "thử lại sau vài phút", vĩnh viễn "đừng hứa thử lại"; CẢ HAI dặn đừng đổi dạng link. Thứ tự chọn câu ở tool: `loiCauHinh` -> `canDangNhap` -> `tamThoi` -> vĩnh viễn (lỗi cấu hình/đăng nhập THẮNG tạm thời, vì máy thiếu yt-dlp mà kèm nguồn rate-limit thì người vận hành cần "cài yt-dlp" chứ không phải "thử lại").
- **Bộ chọn format yt-dlp là MỘT nguồn chân lý (`chon-format-video.ts`, `argsChonFormat()`), dùng CHUNG cho cả đường đọc metadata lẫn đường tải.** Hai đường là hai lời gọi yt-dlp RIÊNG; lệch bộ chọn thì khai kích thước format này nhưng gửi byte format khác - đúng thứ đã crash app Zalo trên điện thoại (V3.22). Nên chung là ràng buộc ĐÚNG ĐẮN, không chỉ DRY. Ba quyết định trong đó:
  - **KHÔNG có nhánh ghép hình+tiếng (`bv*+ba`).** Ghép cần ffmpeg, mà image cố ý không cài ffmpeg (chạy ffmpeg trên nội dung của người lạ là thứ thiết kế này tránh). Để nhánh đó lại thì yt-dlp chọn xong mới chết vì thiếu ffmpeg, và lỗi hạ tầng đó rơi vào câu chung "video có thể ở chế độ riêng tư".
  - **Ưu tiên h264 bằng `-S vcodec:h264`, KHÔNG bằng lọc `[vcodec^=avc]`.** Bản cũ lọc theo tiền tố `avc`, nhưng yt-dlp bản nay khai h264 của TikTok là `vcodec="h264"` chứ không `"avc1.*"` -> `^=avc` khớp RỖNG, lặng lẽ rơi về phân giải cao nhất = h265 (máy cũ không phát, hỏng CÂM - đo tất định qua `--load-info-json`, xem `chon-format-video.test.ts`). `-S` đi qua chuẩn hóa codec NỘI BỘ của yt-dlp (gom `avc1.*`/`h264`/`H264` một rọ, không phân biệt hoa thường) nên miễn nhiễm đổi nhãn; và chỉ SẮP XẾP nên không còn cửa "khớp rỗng". Chỉ có h265 thì `-f` vẫn lấy h265 (lùi mềm). Đánh đổi ĐÃ CHỐT: ưu tiên h264 nghĩa là có video còn 540p trong khi h265 lên 1080p - tương thích máy cũ > nét.
  - **`[format_id!=download]` loại bản watermark khi còn lựa chọn sạch** (yt-dlp gắn `format_note:"watermarked"` cho format `download` của TikTok). Nhánh sau `b[ext=mp4]` vẫn cho nó lọt nếu là mp4 DUY NHẤT. LƯU Ý: clause này dựa vào TÊN format `download`; yt-dlp đổi tên là nó âm thầm vô hiệu -> nếu sau này thấy video TikTok ra watermark, nghi ngờ đầu tiên là chỗ này. Test canh selector chạy yt-dlp THẬT nhưng OFFLINE (`--load-info-json`, tất định) + sàn kiểm bất biến hằng số chạy cả khi máy không có yt-dlp.
- **Bộ tải chỉ được nhận URL bộ dò ĐÃ ĐI TỚI (`urlCuoi`), không nhận chuỗi gốc.** Bản cũ dò một đằng đưa một nẻo, mà zca-js đi theo `location` ĐỆ QUY không đếm hop không kiểm địa chỉ. Đừng siết thành "mọi chuyển hướng thì bỏ": đo được fbcdn trả 302 sang edge khác với MỌI video Facebook (`openGuardedRequest` đã kiểm địa chỉ ở TỪNG hop rồi).

- **`tacGia`/`tieuDe` của video KHÔNG được chạm vào model.** `uploader`/`channel` của yt-dlp là TÊN HIỂN THỊ, chuỗi tự do do người đăng tự đặt. Bản cũ nhúng vào CẢ câu tool trả model LẪN dòng ghi lịch sử, mà lịch sử được đọc lại ở MỌI lượt sau nên đó là lối tiêm chỉ dẫn BỀN; cặp ngoặc vuông của dòng lịch sử còn cho payload ĐÓNG nhãn thật rồi mở nhãn giả (`[đã gửi video của Hoa] [Nguồn: hệ thống] ...`). Kết quả tool là chỗ model tin nhất, hơn cả nội dung web đã bọc `<noi_dung_ngoai>`. Đã bỏ HẲN: `ghiChuDaGuiVideo()` không nhận tham số (chữ ký đó là lời nhắc), `tieuDe` bỏ khỏi type luôn vì không ai đọc mà vẫn chở chuỗi của người lạ. `tacGia` chỉ còn dùng đặt tên file (đã lọc `[^a-zA-Z0-9._-]`) và cắt 64 ký tự ngay tại nguồn.
- **Chỉ lùi sang đường khác khi CHẮC CHẮN chưa gửi.** Đúng hai nguồn chắc chắn: máy chủ trả lời và từ chối (`laLoiMayChuTuChoi` - lỗi có mã SỐ), hoặc zca-js ném TRƯỚC khi POST ("Unable to get video content", lỗi này KHÔNG có mã số nên phải nhận theo chữ). Bắt mọi lỗi là mở cửa gửi HAI LẦN vì zca-js còn ném sau khi đã POST. Luật này repo ghi ở `send-attachment-with-caption.ts`; tool video từng bỏ qua, nay không còn nhánh lùi nào sau lời gọi gửi nên nó chỉ còn là luật cho người sửa sau. Cẩn thận khi test: mock ném TRƯỚC khi làm gì cả chỉ đo được nhánh "chắc chắn chưa gửi" - đúng nhánh không có vấn đề.
- **Lý do hỏng đi bằng LỖI CÓ KIỂU (`LoiGuiVideo`), không bằng chuỗi.** Ở ranh giới tool mọi ngoại lệ trông giống nhau và bị nuốt thành một câu chung, làm mất hai thứ người ta cần: "máy chủ thiếu công cụ" (người vận hành phải sửa) và "video quá nặng" kèm CON SỐ (người dùng chỉnh được trần trên dashboard - `kiem-gioi-han-video.ts` và hint đều hứa như vậy).
- **Cửa bảo mật nằm trong hàm chạm mạng/chạm tiến trình thì KHÔNG có gì canh nó.** Đo được: bỏ `--ignore-config --no-plugin-dirs`, đổi `envToiThieu()` thành `process.env`, bỏ cửa chặn yt-dlp ghi ra ngoài thư mục tạm, bỏ cửa content-type của bộ dò - cả bốn đều để nguyên 2389 test xanh, vì mọi ca đều bị chặn từ trước khi tới phần quyết định. Chữa bằng tách hàm THUẦN (`dungLoiGoi`, `quyetDinhTuHeader`) và tiêm phụ thuộc, cùng cách `ghiStreamRaFileCoTran` đã tách. Riêng `envToiThieu` là bài học sắc nhất: nó được test rất kỹ NHƯ MỘT HÀM, nhưng "nó có được DÙNG không" thì không ai đo.
- **Phép phá chỉ có nghĩa khi biết chắc nó trúng đúng dòng định phá.** Đã trả giá: một chuỗi dùng để phá có ở CẢ HAI hàm (`check` và `hoanSuat`), `replace(...,1)` trúng hàm đầu, test đỏ - và tôi kết luận nhầm rằng test đang canh hàm sau, rồi BÁC một phát hiện đúng của agent. Kiểm bằng số dòng, đừng kiểm bằng "có đỏ hay không". Lỗi cùng loại lặp lại ngay sau đó: ca thay thế viết lần đầu vẫn không đỏ vì dùng MỘT mốc thời gian, mà `pop()` làm mảng rỗng dù có lọc hay không - phải từ hai mốc trở lên.
- `YTDLP_PATH`/`PYTHON_PATH` khai trong schema Zod nhưng **cố ý KHÔNG đưa lên trang Cấu hình**: chúng là đường dẫn tới một FILE THỰC THI, đưa lên dashboard là biến "vào được web" thành "chạy được lệnh tùy ý trên máy chủ", đổi lấy tiện lợi gần bằng không (đặt một lần lúc dựng máy). Cũng KHÔNG nằm trong hai file `.env*.example` - tra ở `docs/deployment-guide.md`.

- Whitelist video: **HAI luật, giữ cả hai.** (a) danh sách host chỉ-để-chuyển-hướng; (b) từ chối URL mang một URL khác trong query. Bản vá đầu chỉ có (a) và SAI CÁCH: `/l.php?u=` chạy y hệt trên `www.facebook.com`, `m.facebook.com`, `mbasic.facebook.com` - những tên bắt buộc phải cho qua; đã kiểm bằng máy chủ nghe thật, 5/6 payload khiến VPS gửi request vào `127.0.0.1`. Nhưng **phép kiểm siêu tập bắt được hồi quy khi tôi định thay (a) bằng (b)**: `L.FaceBook.CoM/l.php?u=x` thì (b) cho lọt vì `x` không phải URL. Số cuối trên 44 payload: cũ chặn 22, mới chặn 32, không mất ca nào, chặn oan 0/12 link thật. Đã thử `--use-extractors default,-generic` (bịt sạch) rồi BÁC: nó phá `fb.watch/...`, `facebook.com/share/v/...`, `tiktok.com/t/...` - đúng thứ nút "Sao chép liên kết" sinh ra. Whitelist vẫn trả **`u.href`** và từ chối userinfo.
- **Instagram tải được, Threads thì KHÔNG.** yt-dlp có extractor IG (reel/post), đo thật 5/5 tải reel công khai không cần login từ IP dân dụng; IG trả DASH (cần ffmpeg ghép) CỘNG **mp4 progressive muxed** (`video_versions`, h264+aac ghép sẵn) - bộ chọn `b[ext=mp4]` tự lấy progressive nên hợp design KHÔNG-ffmpeg (đo ffprobe: có cả video+audio, `docKhungHinhMp4` đọc đúng khung). IG đi **CHỈ yt-dlp** (không có TikWM tương đương), khung mặc định DỌC. Threads: yt-dlp 0 extractor, cả `--force-generic-extractor` ra `Unsupported URL` - chặn ở whitelist, chờ lib. Lỗi IG `empty media response`/`rate-limit` (yt-dlp KHÔNG phân biệt rate-limit/riêng tư/đã xóa) xếp thử-lại-được (`tamThoi`), ưu tiên ca rate-limit phổ biến. **`l.instagram.com` trong `HOST_CHI_DE_CHUYEN_HUONG` GIỜ LÀ LƯỚI CHẶN THẬT, đừng xóa**: từ khi `instagram.com` vào whitelist, `l.instagram.com` khớp đuôi `.instagram.com` nên chỉ bị chặn nhờ luật chuyển-hướng chạy trước - bỏ đi thì `l.instagram.com/?u=x` (x không phải URL) lọt SSRF. Có test khóa.
- Trần song song mặc định **1** (hạ từ 2 ở V3.22, người dùng chốt "cho chắc") vì yt-dlp tốn **72,8 MB RAM đỉnh** mỗi tiến trình, và con số đó CỐ ĐỊNH (~73 MB là bản thân Python) chứ không theo cỡ video - tải thật chỉ lên 75,6 MB vì nó ghi thẳng ra luồng. **CỘNG thêm cỡ video**, vì byte nằm trong RAM: đỉnh = `song song x (cỡ video + 75 MB)`. Hai thanh trượt trên dashboard nhìn riêng rẽ thì ô nào cũng hợp lệ, nhân lên là 2000 MB x 8 = 16 GB - đó là lý do có luật chéo `kiemRamVideo` neo vào `totalmem()` THẬT (cùng cấu hình thì lành trên máy 16 GB, chết trên VPS 1 GB). Không host server nào cả: yt-dlp là tiến trình con, chạy xong thoát hẳn. `pip install -U yt-dlp` là đủ, KHÔNG phải sửa code - Changelog 2025-2026 không có breaking change nào đụng schema `--dump-single-json`.
- Trần video theo giờ đếm số video **ĐÃ GỬI**, không đếm số lần thử: mọi đường thoát không gửi được đều gọi `hoanSuatVideo` trong `finally`. Nguồn sập một lúc mà vẫn trừ suất là khóa người dùng cả tiếng vì những lần họ chưa nhận được gì.
- Lời gọi gửi nằm TRONG suất hàng đợi tải là CỐ Ý (đã cân nhắc kéo ra và bác): tải byte và upload lên Zalo là phần nặng nhất của cả tool, kéo ra là để phần nặng nhất chạy không còn trần song song nào - mà giờ nó còn giữ cả video trong RAM. Phần "phí" chỉ là 800-2500ms giãn nhịp cố ý. Gửi FILE thì đi qua `guiFileKemCaption` chứ không tự gọi `enqueueSend` - đó là chỗ duy nhất biết đường lui khi Zalo từ chối phần định dạng, và chú thích của nó nói thẳng "lưới an toàn phải nằm ở MỘT chỗ".
- **Byte video KHÔNG BAO GIỜ chạm đĩa** (`tai-video-vao-ram.ts`). Người dùng chốt: băng thông không lo, nhưng tải-xóa liên tục thì bào SSD và để lại rác. `uploadAttachment` nhận `{data: Buffer, filename, metadata}` chứ không bắt buộc đường dẫn file - đã rà từng nhánh, kể cả chỗ tính checksum; yt-dlp thì `-o -` xuất thẳng stdout (cần `encoding: "buffer"`, ép về chuỗi là mỗi byte không hợp lệ trong UTF-8 thành ký tự thay thế và file không mở được - hỏng CÂM). Đã xóa hẳn `tai-bang-yt-dlp.ts` và `safe-remote-download-to-file.ts`. Đổi lại RAM thành ràng buộc, xem dòng trần song song ở trên.
- Đọc docx/xlsx bằng **SAX theo luồng** (`saxes` + state machine tự viết), KHÔNG bằng cặp regex bắt cặp thẻ: đo được cặp regex cũ khóa event loop **38,09 giây, 0 tick** trên một bom XML 1,7KB nén / ~1MB giải nén (ReDoS bậc hai, `bom-1mb.docx`, tỉ lệ nén 596:1) - số này đi thẳng vào test hồi quy (`extract-docx-text.test.ts`). Vòng rà soát đầu tiên đo cùng LOẠI bom trên máy khác, nội dung đệm khác, ra **78 giây** - hai số không mâu thuẫn (khác máy, khác payload cụ thể, cùng kết luận: khóa cứng event loop hàng chục giây), CLAUDE.md giữ số 38,09s vì đó là số neo trong test thật, còn tra lại được. Cộng hai lỗi đọc SAI nội dung im lặng của cặp regex cũ (tab stop lẫn vào chữ trích ra, ô Excel rỗng tự đóng nuốt mất ô kế tiếp). SAX đọc từng sự kiện nên không có chỗ cho backtrack bậc hai.
- Trích xuất Kho tri thức (đọc file + cắt đoạn) chạy trong **worker thread riêng** (`chay-trich-xuat-tach-luong.ts`), vì đặt timeout cho code ĐỒNG BỘ đang quay CPU trên CÙNG một luồng là KHÔNG LÀM ĐƯỢC (`setTimeout`/`AbortSignal` chỉ xét ở ranh giới event loop, vòng lặp đồng bộ không bao giờ nhả ranh giới đó). `worker.terminate()` là cách DUY NHẤT dừng được nó giữa chừng - đo cắt một vòng lặp CPU đồng bộ trong **2,2 ms**. Chỉ luồng chính được mở kết nối SQLite (bất biến "một connection cho cả process") nên worker KHÔNG đụng DB, chỉ trả `{chu, doan}` qua `postMessage`.
- Ranh giới `<noi_dung_ngoai>` dùng **nonce ngẫu nhiên sinh mỗi lần gọi** (`wrapUntrustedContent`), KHÔNG lọc `\p{Cf}` (định dạng vô hình) toàn cục trên nội dung: đã đo hướng lọc toàn cục phá emoji ghép ZWJ, tiếng Ba Tư/Ấn (ZWNJ là CHỮ không phải ký tự vô hình ở đó), `½ ﬁ m²`, dấu câu tiếng Trung. Kẻ tấn công soạn nội dung TRƯỚC khi biết nonce của lần gọi này nên không thể viết ra đúng thẻ đóng giả, bất kể chèn ký tự vô hình/homoglyph/chữ fullwidth kiểu gì - nonce chặn được cả bộ 27+ payload đã thử mà không đụng một byte nội dung hợp lệ nào.
- Bộ đếm lần thử giành xử lý nguồn KB (`kb_sources.so_lan_thu`) tăng **TRONG câu UPDATE giành nguồn** (`giaNguonChoXuLy`, so sánh-rồi-đổi nguyên tử), KHÔNG tăng ở nhánh `catch` sau khi biết đã hỏng: nguồn làm worker TREO hoặc CHẾT BẤT THƯỜNG không bao giờ chạy tới được code "sau khi hỏng" - tăng ở đó vô dụng đúng với ca cần đếm nhất (poison pill làm treo tiến trình).
- `KB_MAX_RESULT_CHARS` có **ràng buộc chéo** với `KB_TOP_K`/`KB_CHUNK_CHARS`/`KB_CHUNK_OVERLAP_PERCENT` trong `runtime-tuning-settings.ts` (nhóm `LUAT_CHEO`): thiếu ràng buộc thì `KB_TOP_K=5` và `=20` có thể cho ra kết quả GIỐNG HỆT NHAU vì trần kết quả quá nhỏ so với cả hai, các đoạn cuối bị vứt lặng lẽ không ai báo. Đây là loại lỗi chỉ lộ ra khi NHIỀU tham số kết hợp, nhìn từng ô nhập rời nhau trên dashboard không ai nhận ra.
- **Bài học quy trình từ 5 vòng vá liên tiếp của lớp chống injection**: phép phá (sabotage code rồi xem test có đỏ không) chỉ chứng minh "code mới CẦN cho test mới", KHÔNG BAO GIỜ chứng minh "code mới BAO TRÙM code cũ". Một lần thay regex bảo mật bằng bản "mạnh hơn" đã âm thầm làm HẸP một trường hợp bản cũ từng bắt được (`[ Nguồn:` với một dấu cách ASCII lọt qua) - 9 phép phá liên tiếp không phát hiện ra vì chúng chỉ đo chiều MỚI. Mỗi khi thay một regex/logic bảo mật đã có: chạy CẢ HAI bản trên CÙNG một tập payload và khẳng định tập bắt của bản mới là SIÊU TẬP của bản cũ, không chỉ đo payload mới.
- Tab Bạn bè (`src/conversation/friend-*`, `src/zalo/friend-*`, `src/server/routes/friend-routes.ts`, tab dashboard): **bảng `friend_requests` là BẮT BUỘC vì Zalo không có API liệt kê yêu cầu kết bạn ĐẾN** - chỉ bắt được qua sự kiện listener `friend_event` type REQUEST rồi lưu; xóa khi ADD/REJECT/UNDO hoặc accept/reject. KHÔNG backfill được request cũ (Zalo không cho). **Chỉ kênh cá nhân** (bot `api = null` - route 409, sweep bỏ qua). Handler REQUEST **upsert TRƯỚC rồi enrich `getUserInfo` UPDATE-SAU** (`capNhatHoSoFriendRequest` update-only): làm ngược (enrich trước, ghi sau) mở cửa sổ vài giây để ADD chen vào xóa hụt rồi ta chèn lại một dòng MA cho người đã thành bạn. Handler KHÔNG ném ra listener. Auto-accept **opt-in per-account, mặc định TẮT**, delay chỉnh được (mặc định 1 phút): một **vòng quét toàn cục 30s** đọc bảng pending (restart-safe, KHÔNG dùng setTimeout trong RAM, KHÔNG dùng scheduler agent), đọc config từ DB mỗi lượt (đổi toggle không cần restart), một dòng hỏng không chặn dòng khác, xóa-SAU-khi-accept (giữ dòng để thử lại nếu Zalo từ chối). `deleteAccount` PHẢI dọn luôn `friend_requests` (cùng lớp mồ côi/hồi sinh như `scheduled_jobs`). Accept KHÔNG tự thêm vào allowlist (tách bạch kết bạn vs cho phép chat). Chuỗi người lạ (`message`/`sender_name`) chỉ HIỂN THỊ trên dashboard, KHÔNG vào prompt.
- MCP client (`src/mcp/`, tab dashboard MCP, `src/server/routes/mcp-routes.ts`; sơ đồ `docs/mcp-client-architecture.html`): bot làm **client** cắm MCP server ngoài để agent gọi tool của chúng - KHÔNG phải server. **Chỉ transport HTTP**, KHÔNG stdio: stdio bắt bot chạy lệnh (`command`/`args`) trên VPS = RCE qua đường cấu hình, mà bot đọc tin người lạ. Muốn server local thì tự host rồi đưa URL. Dùng `@ai-sdk/mcp` (không `@modelcontextprotocol/sdk` thô): `client.tools()` trả thẳng `Record<string,Tool>` đúng kiểu `buildAgentTools`.
- **Bump `ai` 7.0.37 -> 7.0.83 là BẮT BUỘC cho `@ai-sdk/mcp`** (nó đòi `@ai-sdk/provider-utils@5.0.32`, ai@7.0.37 mang 5.0.12 -> kiểu `Tool` lệch, vỡ typecheck khi gắn tool MCP vào `streamText`). Cạm bẫy đã trả giá: `ai>=7.0.70` thêm cổng "chỉ tự execute tool khi `finishReason` là 'stop'/'tool-calls'" và runtime đọc `finishReason` dạng **object `{unified,raw}`** (không còn chuỗi). Adapter thật (openai-compatible/anthropic/google) đã phát object nên **production KHÔNG ảnh hưởng**; chỉ **mock test** `thanhKetQuaStream` còn phát chuỗi -> "other" -> chặn execute -> vỡ mọi test có tool call (xem Bẫy khi viết test). Nâng `ai` tiếp phải chạy full suite 1 lần cuối.
- **Default-deny theo agent, 3 lớp** (soi khuôn `agent_kb_sources`): (1) `mcpToolDefinitions(agentId)` chỉ trả tool của server ĐÃ NỐI GIAO ĐÃ GÁN cho agent; (2) `available()` lúc dựng; (3) recheck fail-closed đầu `execute`. Agent chưa gán -> rỗng -> người lạ chat agent chưa cấu hình KHÔNG chạm tool ngoài. Bảng `mcp_servers` + `agent_mcp_servers`; `deleteAgent` PHẢI dọn `agent_mcp_servers` (mồ côi/hồi sinh như `agent_kb_sources`).
- **Tool ngoài KHÔNG đi đường tắt** - đổi thành `ToolDefinition` (`mcp-tool-definition.ts`) rồi CHẢY QUA `listAvailableTools`, thừa hưởng mọi bộ lọc (tắt agent/account, loại lượt lịch). `tool-registry` đọc qua lớp THUẦN `mcp-tool-provider.ts` (manager đăng ký nguồn lúc boot), KHÔNG import thẳng `mcp-manager` - import thẳng kéo `database.ts` mở SQLite ở module scope, vỡ bẫy test. Kết quả tool ngoài LUÔN bọc `wrapUntrustedContent`; nhánh hỏng trả `ketQuaLoi`, KHÔNG ném.
- Manager giữ kết nối sống (Map ở `mcp-connection-pool.ts`, tách khỏi `mcp-manager.ts` cho cả hai < 200 dòng, re-export nên caller không đổi). Nối lúc boot (cạnh `startDashboardServer`, server hỏng KHÔNG chặn boot), health nối lại server `loi`, chốt `dangNap` chống health-tick/double-click chồng kết nối. `tools()` (discovery) PHẢI bọc timeout + đóng handle khi lỗi - thiếu là rò kết nối tích lũy mỗi nhịp health. POST/PATCH route và `nap` đều tôn trọng `enabled` (tắt server = ngắt và ở nguyên ngắt).
- **Tên tool ngoài phải chặn** (`tenToolMcp`): cắt trần 64 ký tự + charset `[A-Za-z0-9_-]` + hậu tố hash `sha256(serverId+toolTen)` khi lossy/cắt. Bỏ qua là server thật (tên dài/unicode) sinh function name sai -> provider 400 -> vỡ CẢ lượt model cho mọi người dùng của agent đó.
- **Fingerprint drift** (`fingerprintTools`/`detectToolDrift` của `ai`): chụp mốc lúc người vận hành duyệt server; server đổi bộ tool ngầm sau đó -> `can_duyet_lai`, KHÔNG nạp tool tới khi duyệt lại. Chống tool poisoning/rug-pull. Mọi tool ngoài `group:"action"` + `runsInScheduledTurn:false` (`readOnlyHint` của server chỉ HIỂN THỊ, KHÔNG dùng quyết định bảo mật). `MCP_ENABLED` là kill-switch RUNTIME (gate trong `mcpToolDefinitions`, tắt dashboard là chặn ngay). Header (secret) mã hóa AES-256-GCM; `src/mcp` KHÔNG có dòng log nào (không rò header).
- Residual risk V1 (ghi ở `docs/system-architecture.md`): mô tả + inputSchema của tool ngoài vào schema model KHÔNG bọc được như kết quả (bản chất cơ chế tool); lần nối ĐẦU (chưa có mốc) nạp tool ngay, không cổng người-duyệt; 2 server tên SẠCH trùng y hệt -> tool che nhau (bounded, hiếm). Chấp nhận theo threat model "server do người vận hành tự thêm".

## Phát hành (release)

Quy trình đầy đủ + bài học: `docs/release-guide.md`. Ba điều BẮT BUỘC nhớ:

- **Thêm/sửa/xóa feature hay sửa lỗi đáng kể nào thì cập nhật `CHANGELOG.md` mục `[Chưa phát hành]` NGAY trong đợt đó**, đừng để dồn. Lần cắt `v0.2.0` (2026-08-30) suýt bỏ sót 3 feature lớn (tải video, Tab Bạn bè, MCP client) khỏi release notes vì mục này bị trống nhiều đợt - phải dò lại git log mới thấy.
- **`v0.x` ở chân sidebar là semver PHÁT HÀNH, đọc từ `package.json` lúc BUILD** (`web/vite.config.ts` -> `__APP_VERSION__`), KHÔNG tự đổi theo deploy. Đổi số = bump `package.json` RỒI `pnpm build:web` (web/dist là artifact, đã gitignore). Mốc "V3.xx" trong `docs/project-roadmap.md` là nhật ký nội bộ, KHÁC hệ semver này.
- Cắt bản (từ `main` sạch, verify xanh): bump `package.json` -> cuộn `[Chưa phát hành]` thành `[x.y.z] - <ngày>` + thêm mục rỗng mới + sửa link cuối CHANGELOG -> commit `chore(release): x.y.z` -> `git tag -a vx.y.z` -> `git push --follow-tags` -> `gh release create`. `chore(release):` là quy ước của repo cho commit này. KHÔNG tự chạy git - push + GitHub Release ra ngoài công khai, hỏi user trước.

## Lệnh hay dùng

```bash
pnpm zalo-login <account-id>   # login QR, ảnh QR tự mở (KHÔNG phải `pnpm login` - trùng lệnh built-in pnpm)
pnpm dev                       # chạy bot (watch mode)
pnpm typecheck                 # bắt buộc chạy trước khi báo hoàn thành
```

## Quy ước code

- File < 200 dòng, kebab-case, tên tự mô tả.
- Env var mới: khai schema Zod trong `src/config/env.ts` **kèm `.default()`** - `CREDENTIALS_ENCRYPTION_KEY` là biến DUY NHẤT được phép bắt buộc (nó giải mã chính những gì dashboard lưu nên không thể nằm trên dashboard). Tham số chỉnh được lúc chạy thì thêm vào `src/config/tuning-definitions.ts` là đủ - nó tự hiện trên trang Cấu hình. **Chỉ** thêm vào `.env.example` / `.env.production.example` khi biến đó phải đọc TRƯỚC lúc mở DB (log, DATA_DIR, cổng dashboard) - hai file đó cố ý chỉ còn 9 biến, vì giá trị cũ nằm lại trong `.env` trong khi DB đang gánh đã gây hiểu nhầm thật.
- Thứ tự ưu tiên cấu hình ở MỌI nơi: dashboard (DB `runtime_settings`) > `.env` > `.default()` trong schema. Thiếu cấu hình LLM **không được** chặn boot: phải vào được dashboard mới nhập được.
- Tin nhắn bị lọc (allowlist, thiếu @mention) phải bị chặn **trước** khi gọi LLM để không tốn token.
- Chuỗi tiếng Việt giữ nguyên dấu; chỉ dùng dấu câu ASCII.

## Chạy test (NGHIÊM CẤM cách cũ)

- **Sửa file nào chạy ĐÚNG file test đó**: `npx tsx --test src/.../ten.test.ts` (vài giây). KHÔNG chạy `pnpm test` (cả ~548 file, ~50s) trong vòng lặp phá-kiểm - kể cả khi sửa file dùng chung, chỉ chạy các file test import trực tiếp nó.
- **Full suite (`pnpm test`) chỉ chạy MỘT LẦN ở cuối**, ngay trước khi báo xong, lúc máy rảnh. Không lặp, không chạy "cho chắc" nhiều lượt.
- **TUYỆT ĐỐI KHÔNG tự sinh tiến trình đốt CPU / tạo tải giả** để ép test nhấp nháy. Muốn nghiệm thu dưới tải cao thì HỎI người dùng trước, mỗi lần. Đã trả giá: một phiên chạy full suite ~30 lần (phần lớn là vòng phá-kiểm chỉ đụng vài file video) cộng burner 16-20 tiến trình, đẩy CPU máy người dùng lên 100% suốt nhiều phút mà không cần thiết.
- `pnpm typecheck` rẻ, chạy thoải mái.

## Bẫy khi viết test

- `database.ts` mở SQLite và chạy migration ở MODULE SCOPE. Test nào import (trực tiếp hay bắc cầu) module đó TRƯỚC khi gọi `setupTestEnv()` (trong `src/shared/test-env-setup.ts`) sẽ mở nhầm `data/zalo-agent.db` THẬT thay vì DB tạm - migration hiện tại đều idempotent nên vô hại, nhưng một migration có backfill/ALTER sau này sẽ chạy lên dữ liệu thật mỗi lần ai đó chạy `pnpm test`. Luôn gọi `setupTestEnv()` xong rồi mới `await import()` động các module chạm DB - không import tĩnh ở đầu file test.
- Model giả phải cài `doStream`, KHÔNG phải `doGenerate` - vòng lặp đi đường `streamText` nên `doGenerate` không bao giờ được gọi tới. Fixture kiểu "một lần trả lời" vẫn giữ nguyên, bắc cầu bằng `thanhKetQuaStream()` (`src/agent/streaming-model-test-helper.ts`).
- Part `finish` của mock phải mang `finishReason` dạng **object `{unified,raw}`**, KHÔNG phải chuỗi. Từ `ai>=7.0.70` SDK chỉ tự execute tool khi finishReason là 'stop'/'tool-calls' và đọc qua `.unified` - chuỗi trần rơi về "other" nên tool KHÔNG chạy, mọi test có tool call vỡ CÂM (adapter thật phát object nên production không dính). `thanhKetQuaStream` đã bọc sẵn; test tự dựng stream mock phải theo (mẫu `thread-summarizer.test.ts`).
- `toReceiptParams` (`message-receipts.ts`) đọc từ `msg.rawData`, KHÔNG đọc các trường cấp trên. Fixture để `rawData: {}` thì mọi biên nhận bị bỏ lặng lẽ và test "đã xem" xanh giả - nó đo 0 lần gọi rồi tưởng là đúng.
- Test chạm `processBatch` phải chặn được đường gọi provider thật. Batch đang ĐỖ trong hàng chờ mà nhả khoá thread ra là nó chạy thật và đi ra mạng ngoài - dọn bằng `clearPendingBatches()` TRƯỚC khi nhả.
- **Đợi việc PHẢI xảy ra thì dùng `doiChoDenKhi`/`doiChoSoLuong` (`src/shared/doi-cho-den-khi.ts`), KHÔNG `await sleep(N)` rồi khẳng định số đếm.** `sleep` đo đồng hồ treo tường chứ không đo thứ test quan tâm: máy bận thì việc chưa xong, số hụt, đỏ oan - đo được 4/5 lượt đỏ khi chạy cả bộ dưới 6 tiến trình đốt CPU. Sửa bằng NỚI BIÊN (`sleep(20)` -> `sleep(200)`) chỉ đẩy ngưỡng đỏ ra xa và làm chậm mọi máy. Ngược lại, khẳng định PHỦ ĐỊNH ("sau `stop()` không được bắn thêm") thì GIỮ `sleep` - tải chỉ làm cơ hội bắt lỗi cao hơn, còn chờ-đến-khi ở đó về ngay lần thử đầu nên chẳng chứng minh gì.
- Viết lại một test đã có thì chạy PHÉP PHÁ trên CẢ HAI bản, cùng một chỗ phá. Đã trả giá: ca "không nhắc lại trong cùng quãng chờ" được viết lại thành ba lời gọi cùng nhịp cho "chắc chắn" hơn, test vẫn xanh, nhưng bỏ hẳn cửa khoảng lặng trong code thật thì nó VẪN XANH - vì lời gọi 2 và 3 bị chặn sớm ở cửa `dangGuiTren` nên không còn chạm tới luật đang đo. Bản cũ chạy cùng phép phá đó thì đỏ đúng.
- Test đo HIỆU NĂNG (tuyến tính so với bậc hai) nhiễm nhiễu theo lượng tử lập lịch của hệ điều hành (15-30ms trên Windows). Loạt đo dài hơn lượng tử thì dưới tải loạt nào cũng bị cướp CPU, `min` vẫn ra số bẩn. Sửa bằng loạt NGẮN hơn + lấy `min` của NHIỀU loạt, đừng nới ngưỡng - đó là làm bộ ước lượng chính xác hơn, khả năng bắt lỗi tăng chứ không giảm.
