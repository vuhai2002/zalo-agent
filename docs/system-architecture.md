# System Architecture - zalo-agent

## Tổng quan

```
Zalo servers <--ws/https--> zca-js (npm lib, unofficial)
                                |
                    src/zalo/account-manager.ts   (N account, mỗi account 1 api + 1 listener)
                                |
              src/zalo/incoming-message-router.ts (ghi contact/thread, báo "đã nhận")
                                |
              middleware: allowlist-filter -> (lọc xong mới tốn token)
                                |
              src/zalo/message-turn-processor.ts  (1 lượt: agent -> trả lời -> history)
                                |
                    src/agent/agent-loop.ts       (Vercel AI SDK generateText + tools)
                                |                  provider theo env: 9Router / Anthropic
              tools: add_reaction | send_file | tag_member | get_group_info
                                |
              middleware: rate-limiter (queue per thread + delay ngẫu nhiên) -> sendMessage
                                |
              conversation/history-store.ts        (node:sqlite, key account_id + thread_id)
```

## Quyết định kỹ thuật chính

| Quyết định | Lý do |
|---|---|
| Dùng zca-js qua npm, không fork | Update lib bằng `pnpm update`; phần reverse-engineer giao thức để lib gánh |
| Vercel AI SDK thay vì SDK provider trực tiếp | Đổi LLM provider = đổi env, không sửa code (mặc định trỏ router proxy 9Router) |
| `node:sqlite` built-in thay better-sqlite3 | better-sqlite3 cần Visual Studio Build Tools trên Windows (Node 24 chưa có prebuilt); node:sqlite đủ cho history, zero native dep. Trạng thái experimental - nếu Node đổi API thì chỉ sửa 1 file `history-store.ts` |
| tsconfig `moduleResolution: Bundler` | `index.d.ts` của zca-js dùng directory import (`export * from "./dist"`) - NodeNext không resolve được types; Bundler khớp cách tsx/esbuild chạy runtime |
| Cookie mã hóa AES-256-GCM, key từ env | Cookie = full quyền tài khoản Zalo; file layout `[iv][authTag][ciphertext]`, mode 0600 |
| Filter trước agent | Tin bị lọc (không @mention, ngoài allowlist, tin của bot) không bao giờ chạm LLM -> không tốn token |
| Tool send_file giới hạn `data/shared-files/` | Chặn agent đọc file tùy ý trên máy (vd credentials) qua prompt injection |
| Ghim `zca-js` đúng version (không dùng `^`) | Lib reverse-engineer, minor bump có thể đổi hành vi. Nâng cấp phải là hành động có chủ đích + chạy lại test, không để `pnpm update` nhảy âm thầm |
| Không bọc facade quanh zca-js | Chỉ dùng 8/150+ hàm, phần lớn import là `type API`. Đổi chữ ký hàm thì TypeScript bắt ngay lúc typecheck - thêm tầng trung gian là over-engineering (chỉ đáng khi thêm kênh khác, mà V3 đã chốt không làm) |
| `web_fetch` đi theo chuỗi 2 tầng: tự fetch trước, Jina Reader đỡ sau | Đúng pattern của cả hai repo tham khảo (GoClaw `ExtractorChain`: Defuddle hosted -> bóc in-process; Hermes: Firecrawl gateway -> provider khác). Tầng 1 nhanh, riêng tư, miễn phí và thừa hưởng guard SSRF; tầng 2 render được trang JavaScript và qua được một phần chặn bot. Đo thực tế: `giavang.doji.vn` tầng 1 chỉ ra 11 ký tự (trang render JS) - Jina cứu được 938 ký tự; `vnexpress.net` từ HTTP 406 thành chạy ở tầng 1 sau khi thêm header trình duyệt. Đánh đổi của tầng 2: chậm (1-16s) và URL đi qua bên thứ ba, tắt bằng `WEB_FETCH_FALLBACK_ENABLED`. Jina trả HTTP 200 KỂ CẢ khi trang đích chặn nó nên phải tự soi dòng `Warning: Target URL returned error` |
| Request tự fetch phải mang header trình duyệt | Request trần không header bị nhiều site trả 403/406 (đo được: vnexpress 406, sjc.com.vn 403). Bộ User-Agent + Accept + Accept-Language tối thiểu là qua được phần lớn; site sau Cloudflare vẫn chặn nhưng đó là việc của tầng 2 |
| Cấu hình chuỗi nguồn nằm trong modal mở từ nút Settings TRÊN DÒNG TOOL, không phải khối rời | Bản đầu đặt cấu hình search thành một khối riêng cuối trang Tools - người dùng phản hồi "2 nơi khó hiểu". Theo đúng mẫu "Extractor Chain" của GoClaw: nút Settings nằm trên dòng tool, modal liệt kê các bậc theo thứ tự thử (#1, #2) kèm mô tả và toggle từng bậc. Bậc CUỐI khoá cứng (hiện nhãn "Luôn bật" thay cho toggle) - đó là thứ bảo đảm tool không rơi vào trạng thái "chưa cấu hình" |
| Cấu hình nguồn search nằm trong DB, sửa từ dashboard; DuckDuckGo là lưới đỡ KHÔNG tắt được | Key Brave nhập ở trang Tools (mã hóa AES-256-GCM như key LLM, `env.BRAVE_SEARCH_API_KEY` chỉ là dự phòng khi DB trống) để đổi key không phải sửa file env rồi restart. Chọn Brave mà chưa có key bị chặn ngay ở tầng store nên API và mọi caller khác đều dính cùng luật; mất key thì tự hạ về DuckDuckGo thay vì kẹt ở trạng thái gọi Brave với key rỗng. DDG luôn đứng cuối chuỗi nên web search không bao giờ ở trạng thái "chưa cấu hình" |
| Prompt caching bật bằng MỘT header `x-session-id` ổn định theo thread, không phân nhánh theo provider | Đọc source 9Router (`open-sse/utils/sessionManager.js`): router suy khóa cache theo thứ tự header `x-session-id` -> `body.prompt_cache_key` -> FALLBACK băm text assistant trong request. Client câm thì rơi vào fallback, mà text assistant dài thêm mỗi lượt nên khóa đổi liên tục, cache không bao giờ trúng (đo được `CACHED TOKENS: 0` trên 181k token). Chọn header vì nó là đường CHUNG: `codex.js`/`grok-cli.js` đổ vào `prompt_cache_key`, `claude.js` dùng giữ fingerprint khi cloaking, kiro/antigravity dùng định danh phiên, provider thường bỏ qua vô hại - đổi provider trên router không phải sửa code bot. Khóa băm sha256 chứ không ghép id thô: thread id là định danh người dùng thật, không nên rò vào log của router |
| KHÔNG tự gắn `cache_control` cho đường Anthropic | `open-sse/translator/formats/claude.js` XÓA sạch `cache_control` client gửi lên rồi tự đặt lại (system block cuối ttl 1h, assistant cuối, tools). Client gắn thêm chỉ tốn công - router lo phần đó |
| Lấy ảnh cỡ `normal` từ payload Zalo thay vì `hd`, `HISTORY_IMAGE_CONTEXT_LIMIT` mặc định 1 | Ảnh là khoản token đắt nhất: đo thực tế lượt có ảnh ~12.600 token input, lượt text thuần ~2.600 - chênh lệch ~10.000 chính là 4 tấm ảnh (1 mới + 3 nạp lại từ history), lại nhân với số step vì mỗi step gửi lại cả hội thoại. Zalo đã resize sẵn nhiều cỡ trong payload (`thumb`/`normalUrl`/`href`/`oriUrl`/`hd`) nên chỉ cần đổi thứ tự ưu tiên, KHÔNG cần thư viện resize (sharp cần build native trên Windows). Hermes cũng resize ảnh nhưng ngưỡng 7900px/4MB - họ chống lỗi 400 của API chứ không tối ưu token |
| KHÔNG dùng `reasoningTokens` để kiểm chứng thinking trên đường gpt-5.6-sol | Đo bằng request thật: body ra router CÓ `reasoning_effort: "medium"`, và `executors/codex.js:441` đổi nó thành `reasoning: {effort, summary:"auto"}` cho backend Codex - thinking chạy thật. Nhưng `translator/response/openai-responses.js:474` gọi `buildUsage` mà không truyền `reasoningTokens`, còn `buildUsage` chỉ thêm `completion_tokens_details` khi > 0 - nên client LUÔN thấy 0. Kết luận cũ ở V2.5.3 ("reasoningTokens = 0 tức router nuốt tham số") là sai: router nuốt phần BÁO CÁO, không nuốt tham số |
| Thinking bật qua `LLM_REASONING_EFFORT`, chokepoint duy nhất `reasoning-options.ts` | Không bật thinking, model lướt 50k token nội dung trang một lượt - đối chiếu số vé, đọc bảng đều ẩu (vụ dò vé: dữ liệu đã vào context mà vẫn "chưa lấy được bảng"). Học Hermes: một nơi resolve tham số reasoning cho mọi đường gọi, map theo provider (openai-compatible: `reasoning_effort` chuẩn; anthropic: adaptive thinking + effort). Kiểm chứng bằng reasoningTokens trong log; = 0 khi đã bật tức router nuốt tham số |
| Retry + fallback khi router trả completion rỗng | 9Router có lúc trả HTTP 200 với message trống (0 token) - SDK coi là thành công nên `maxRetries` không retry, bot im lặng bỏ treo người nhắn. `agent-loop` nhận diện chữ ký rỗng (text trống + 0 tool call + 0 token - phân biệt được với lượt "chỉ thả reaction" hợp lệ vốn có tool call + token), tự retry 1 lần, vẫn rỗng thì trả lời fallback. Gốc bệnh ở router, bot chỉ chống chịu |
| `payload-anomaly-watch.ts` cảnh báo payload lạ | Rủi ro thật không phải zca-js đổi API (TS bắt được) mà là **Zalo đổi tên field** - parser moi payload bằng `any` nên bot chỉ âm thầm ngừng thấy ảnh / ngừng nhận @mention. Tin thiếu `threadId` còn bị bỏ qua không dấu vết. Watcher log warn (throttle 10 phút/loại/account) để lỗi hiện ra thay vì im lặng |
| Mọi lần tải từ URL đi qua `shared/safe-remote-download.ts` | URL của tool `send_file` do LLM quyết, mà LLM đọc tin của người lạ -> prompt injection có thể bảo bot tải `http://127.0.0.1:3900/api/...` hay metadata cloud (`169.254.169.254`) rồi gửi ra chat. Guard chặn IP nội bộ (kiểm cả literal IP trong URL, cả từng hop redirect) và truyền `lookup` đã kiểm vào `http.request` nên socket dùng đúng địa chỉ đã duyệt - chặn luôn DNS rebinding. Đọc theo stream và cắt khi vượt hạn mức, thay vì `res.arrayBuffer()` nạp cả response vào RAM rồi mới kiểm cỡ |
| Rate-limit login tính theo IP socket, chỉ tin `X-Forwarded-For` khi `DASHBOARD_BEHIND_PROXY=true` | Header XFF do client gửi được: đổi giá trị mỗi request là mỗi lần có bucket rate-limit mới -> brute-force password không giới hạn. Khi có proxy thì lấy entry **phải nhất** vì Caddy/Nginx *append* IP client vào cuối chuỗi có sẵn; lấy entry trái nhất (cách hay gặp) chính là lỗ hổng |
| Phiên dashboard lưu bảng `dashboard_sessions` (đổi so với bản HMAC stateless trước đó) | Token tự chứng thực thì logout chỉ xóa cookie phía client - cookie bị lộ vẫn dùng được tới khi hết hạn, và đổi `DASHBOARD_PASSWORD` không đá được phiên nào. Đánh đổi: thêm 1 truy vấn SQLite mỗi request (bảng vài dòng, node:sqlite sync) để logout thu hồi được thật. DB chỉ lưu `sha256(secret)`; cột `password_fingerprint` khiến đổi password là kick sạch phiên cũ |
| File tạm của `send_file` xóa ngay trong `finally`, sweep định kỳ chỉ để bắt file mồ côi | `data/tmp` là nơi trung chuyển, không phải nơi lưu trữ. Sweep 24h một lần chỉ dọn phần sót lại khi process bị kill giữa lượt gửi |
| `LLM_API_KEY` để trống được | Key nhập được ở dashboard (lưu DB, mã hóa) nên bắt buộc lúc boot là vô lý - không cấu hình được từ UI thuần. Thiếu key thì log warn lúc khởi động và lượt agent ném lỗi có hướng dẫn, thay vì `process.exit(1)` |
| Tin của người dùng vào history cả khi lượt agent lỗi | Trước đây `appendMessage` nằm sau `runAgentTurn`: provider chết là tin nhắn biến mất khỏi history, lượt sau bot không biết người ta đã nói gì. Vẫn phải ghi SAU khi agent chạy (agent tự ghép batch vào input, ghi trước thì tin lặp 2 lần) nên dùng cờ ghi-một-lần gọi ở cả 2 nhánh |
| System prompt chỉ bơm NGÀY (không giờ), giờ chính xác qua tool `get_datetime` | Giờ đổi mỗi phút là prompt cache của provider vỡ mỗi phút; chỉ ngày thì cache sống nguyên ngày (pattern GoClaw). Thứ trong tuần do server tính - LLM tự suy thứ từ ngày rất hay sai. Không có dòng ngày model đoán từ training data và trả lời sai ngày hôm nay |
| Web search: chuỗi Brave (khi có key) -> DuckDuckGo (luôn cuối, không key) | Pattern chung của cả GoClaw lẫn Hermes: DDG scraping `html.duckduckgo.com` miễn phí làm fallback bắt buộc nên web search không bao giờ "chưa cấu hình"; Brave free tier 2000 query/tháng cho kết quả tốt hơn, lỗi/hết quota tự rơi về DDG. Provider lỗi trả mảng rỗng cho tool diễn giải, không throw ra agent loop |
| Tool lọc qua HAI lớp giao nhau: agent (`agents.disabled_tools`) và account (`accounts.disabled_tools`) | Cả hai lưu danh sách TẮT thay vì BẬT, để tool mới thêm vào registry tự bật cho bản ghi cũ, không cần migration. Tool dùng được là phần KHÔNG BÊN NÀO tắt: agent khai NĂNG LỰC (agent này biết làm gì), account áp CHÍNH SÁCH (nick này được phép gì). Không bên nào bật ngược lại được bên kia, nên thêm một agent mới không bao giờ nới rộng được quyền của một nick - kể cả agent tạo cẩu thả. Tool tắt không đưa vào schema gửi LLM: không tốn token mô tả, model không biết nó tồn tại, prompt injection không dụ gọi được. Catalog một nguồn ở `tool-registry.ts`, UI đọc qua `/api/tools`. Cùng mô hình chuỗi giao nhau của GoClaw (`internal/tools/policy.go`), khác chỗ Hermes chỉ có lớp nền tảng vì nó chỉ có một agent |
| Ngân sách ngữ cảnh tính bằng TOKEN, không phải số tin (`LLM_CONTEXT_WINDOW`) | Đếm tin không chặn được ngữ cảnh phình vì một tin Zalo dài tùy ý - đo trên dữ liệu thật đã có lượt cộng dồn 184.835 token qua 8 step. Bot chỉ dùng tới 70% trần, chừa cho phần model viết ra và kết quả tool cộng dồn. Vượt thì bỏ ẢNH cũ trước (mỗi tấm bằng cả trăm dòng chữ), rồi mới bỏ tin cũ; không bao giờ cắt tin của lượt hiện tại và không cắt GIỮA một tin (cắt hở sẽ bỏ mất thẻ đóng khối `<noi_dung_ngoai>`). Một hàm `nganSachAnToan` dùng chung cho cắt trước lượt, điều kiện dừng giữa lượt, và lượt chốt - ba chỗ hiểu con số theo hai nghĩa khác nhau từng làm nửa tính năng thành vô dụng |
| Override nhà cung cấp của agent bị BỎ QUA khi lệch cấu hình chung | `apiKey` và `baseUrl` luôn lấy từ cấu hình chung (chưa có key riêng cho từng agent), nên đổi provider mà giữ key cũ nghĩa là gửi khóa của bên này sang endpoint bên kia - lộ credential sang bên thứ ba, và bot câm vì 401. Chặn ở `resolveLanguageModel` (chỗ duy nhất dựng client) chứ không chỉ ở giao diện: bản ghi lệch có thể đã nằm sẵn trong DB, và giao diện không khóa được khi chưa tải xong cấu hình chung |
| `html-to-text` lọc list theo mật độ link, cap fetch nâng lên `WEB_FETCH_MAX_CHARS` (15000) | Lỗi thật vụ dò vé số: trang minhngoc 9438 ký tự text thì 8300 ký tự đầu là menu, cap 8000 cắt đúng trước bảng kết quả -> model thấy toàn menu, tưởng trang không có dữ liệu rồi bỏ cuộc. Tín hiệu Readability tối giản (list nào >= 70% chữ nằm trong link là menu điều hướng, vứt; match list trong cùng trước để gỡ được menu lồng) + decode entity tên (&Ecirc; -> Ê) + ô bảng ngăn bằng " \| ". GoClaw giải cùng bài bằng Defuddle + cap 60000; mình không thêm dependency và để cap 15000 vì mỗi ký tự là token trả tiền |
| Persona có khối "quy tắc tra cứu" chưng cất từ Hermes | Bot có tool nhưng bỏ cuộc ở step 3/8 rồi đẩy việc ngược cho người dùng - đúng failure mode Hermes trị bằng `<tool_persistence>`/`<missing_context>`: thông tin thời sự PHẢI search trước khi nói không biết; trang đầu không có thì thử trang khác/đổi từ khóa; chỉ hỏi ngược người dùng khi tool không lấy được; tool lỗi thì nói thật, cấm bịa. Việc dính tiền (dò vé số, giá) phải nêu nguồn + ngày |
| Tool nhóm theo mức rủi ro read/action | Học từ webhook-safe toolset của Hermes (nguồn không tin cậy chỉ được cấp tool đọc): `read` = tra cứu không tác động, `action` = gửi/sửa trên Zalo. Hiện chỉ để nhóm UI; là nền cho allowlist tool theo thread sau này |
| Câu trả lời dài bị cắt thành nhiều tin, cắt ở ranh giới tự nhiên | Zalo chặn tin dài ở PHÍA SERVER (`error_code 118` "Nội dung quá dài") và zca-js không kiểm gì (`sendMessage.ts` chỉ đếm attachment) - vượt ngưỡng là mất trắng: tin không gửi được, không vào history, lượt đó đốt 175k token ra số 0. `split-long-message.ts` cắt theo thứ tự đoạn văn -> dòng -> hết câu -> khoảng trắng, KHÔNG BAO GIỜ giữa từ; điểm cắt phải lấp >= 50% cửa sổ để không sinh ra đoạn 30 ký tự kèm đoạn 2000. Ngưỡng thật chưa có tài liệu: đo được chính Zalo tự cắt tin dán vào thành đoạn 2613 ký tự (tức trần >= 2613), `zalo-personal` chốt 4000 làm trần cứng và 2000 mỗi đoạn - lấy 2000 cho có biên phòng khi Zalo tính theo byte. Trần 5 đoạn/lượt vì bắn hàng chục tin liên tiếp là hành vi dễ bị đánh dấu spam |
| History chỉ ghi phần ĐÃ gửi được, không ghi nguyên câu trả lời | Gửi hỏng ở đoạn 2/3 mà history ghi đủ 3 đoạn thì lượt sau bot tưởng người dùng đã đọc phần chưa bao giờ tới nơi. Ngược lại thông báo lỗi kỹ thuật thì KHÔNG ghi - nó là thông báo hệ thống, để lại chỉ khiến model neo vào tiền lệ hỏng (đúng bệnh đã trị ở V2.5.2) |
| Lỗi lượt agent phải trả lời người nhắn, không im lặng | Trước đây `catch` chỉ log: provider chết hay Zalo chặn tin là người nhắn thấy chỉ báo "đang nhập" tắt ngóm rồi thôi, không biết nên chờ hay nhắn lại. Dùng chung đúng một câu với nhánh router rỗng để không phải đoán hai thông báo khác nhau nghĩa là hai chuyện khác nhau |
| Báo "đã nhận" cho mọi tin, "đã xem" chỉ cho tin bot xử lý | Bot đọc tin qua listener chứ không mở cửa sổ chat nên Zalo không tự bắn hai sự kiện này - phía người nhắn thấy tin đứng ở "Đã gửi" vĩnh viễn, trông như bot chết. Bám nếp client thật: `sendDeliveredEvent(false, ...)` ngay khi tin về (kể cả tin sắp bị lọc, client thật cũng báo nhận tự động), `sendSeenEvent` khi bot bắt đầu xử lý (giống người mở hội thoại ra đọc). Tin passive-listen KHÔNG báo đã xem: báo xem rồi im lặng khiến người nhắn tưởng bot đang soạn trả lời. Cả hai fire-and-forget như auto-react |
| Mọi response tải về đều giải nén theo `content-encoding` | `node:http` không tự thêm `Accept-Encoding` và cũng không tự giải nén (khác `fetch`/undici), nhưng nhiều site nén gzip kể cả khi không được hỏi - đo được znews.vn trả `content-encoding: gzip` với request không hề khai. Code cũ `.toString("utf-8")` thẳng vào buffer nén nên web_fetch đẩy 46.066 ký tự rác nhị phân vào context (tienphong 19.680), vừa đốt token vừa mất nguồn, mà rác thì dài nên ngưỡng "quá ít chữ" không kích hoạt được lưới đỡ Jina. Sau khi vá: znews ra 10.853 ký tự bài báo thật. Khai luôn `Accept-Encoding` như trình duyệt (bớt một tín hiệu bot) và chặn zip bomb bằng `maxOutputLength` - cap ở tầng stream chỉ đo dữ liệu NÉN, không cứu được file 1MB bung thành hàng GB |
| Ảnh nhận được lưu file `data/media/`, không nhét base64 vào SQLite | URL ảnh Zalo có hạn dùng, còn history chỉ lưu text -> trước đây bot không xem lại được ảnh cũ. Giờ `media-store` lưu file + cột `messages.images` trỏ tới; agent nạp lại tối đa `HISTORY_IMAGE_CONTEXT_LIMIT` ảnh gần nhất (mặc định 3 - mỗi ảnh tốn token mỗi lượt). File quá `MEDIA_RETENTION_DAYS` (7 ngày) bị dọn, history tự rơi về text `[gửi kèm N ảnh]`. Lưu ý: ảnh nằm plaintext trên đĩa (khác cookie có mã hóa) - `data/` đã gitignore |
| Vision phân loại 4 trạng thái `vision\|no-vision\|combo\|unknown`: auto tra `GET {baseUrl}/models` đọc `capabilities.vision` + `owned_by: "combo"`, cache 10 phút theo baseUrl | Model không vision mà vẫn đính ảnh: qua 9Router thì router lột ảnh thay bằng placeholder (bot "mù" không báo trước, base64 vẫn tốn băng thông), endpoint khác thì HTTP 400 chết lượt. `/v1/models` của 9Router trả capabilities cho từng model (cùng nguồn icon con mắt trên UI router) - hỏi router thay vì bảng tay. Combo nhận diện được qua `owned_by` nhưng KHÔNG lộ thành viên (`/api/combos` 401 kể cả kèm key - đã test) -> đối xử riêng bằng chế độ hybrid. Model lạ -> unknown -> coi như CÓ vision (lạc quan, giữ hành vi cũ). Anthropic trực tiếp luôn true. on/off ép tay cho endpoint câm (LM Studio, vLLM). Kiểm chứng sống trên instance thật: đủ 4 nhánh ra đúng |
| Combo + có sidecar -> chế độ `hybrid`: đính CẢ pixel LẪN text mô tả | 9Router auto-switch (`combo.js`, luôn bật) đẩy thành viên vision lên đầu khi LƯỢT HIỆN TẠI có ảnh, nhưng chủ đích không pin combo theo ảnh HISTORY ("History media must not pin the combo") - lượt follow-up không kèm ảnh mới có thể rơi vào thành viên mù, ảnh nạp lại từ history bị lột êm thành placeholder, bot "quên" ảnh không dấu vết. Hybrid chữa đúng lỗ này: thành viên vision thấy pixel (mô tả thừa ~300-500 token), thành viên mù bị lột pixel nhưng text mô tả sống sót. Không có sidecar thì combo giữ native - hybrid không làm được gì hơn |
| Cấu hình đọc ảnh nằm TRONG modal Settings của dòng `read_image` ở trang Tools, không phải card riêng ở Providers | Bản đầu đặt ở Providers với lập luận "sidecar phục vụ cả đường ống chính (describe/hybrid) chứ không riêng tool". User phản hồi 2 nơi khó hiểu và chốt gom về Tools - đúng tiền lệ đã có với cấu hình chuỗi search (V2.6: "2 nơi khó hiểu"). Trình bày lại thành CHUỖI đúng mẫu Extractor Chain: #1 model chính tự đọc pixel (dropdown auto/on/off), #2 sidecar mô tả ảnh thành chữ. Badge chế độ ảnh đang hiệu lực (native/describe/hybrid/blind) hiện ngay trên dòng tool. Trang Providers quay lại đúng một việc: cấu hình LLM chính. Kèm theo: `/api/tools` phải trả `available` + `unavailableHint` vì `buildAgentTools` lọc theo `available()` còn catalog thì không - lệch nhau là UI hiện tool "bật" trong khi model không hề nhận được |
| Mọi ô nhập secret phải có đường XÓA riêng, không dựa vào nút Lưu | Cả key Brave lẫn key sidecar đều quy ước "ô trống = giữ key cũ" (để đổi cấu hình khác không phải nhập lại key), nên nút Lưu KHÔNG BAO GIỜ gửi được chuỗi rỗng - key nằm lại DB vĩnh viễn dù người dùng đã xóa hết các ô khác. Đã dính đúng lỗi này 2 lần ở 2 chỗ. Nút xóa riêng + confirm (mất key thật, phải xin lại từ nhà cung cấp); backend đã sẵn sàng từ đầu (`braveApiKey: ""` xóa row, `clearSidecarSettings`), thiếu mỗi đường gọi từ UI |
| `DELETE /api/vision/sidecar` riêng để gỡ key, và cờ `hasApiKey` trong API view | Đường PATCH quy ước "key bỏ trống = giữ key cũ" (để đổi model không phải nhập lại key) nên KHÔNG có cách nào gỡ key qua PATCH - phải có hành động riêng. `apiKeyMasked` không dùng thay được: chuỗi đó không bao giờ rỗng (trống thì ra "chưa cấu hình") nên UI không suy ra được có key hay không, dẫn tới nút xóa hiện cả khi không có gì để xóa |
| Tool `read_image(question, imageIndex)` - agent tự "nhìn kỹ lại" ảnh qua sidecar, chỉ vào schema khi sidecar cấu hình (registry `available()`) | Mô tả cache sinh TRƯỚC khi biết câu hỏi với prompt cố định nên lossy: "đếm số cá màu vàng" không có trong mô tả là model chính bó tay. Cùng lời giải với GoClaw (`read_image`) và Hermes (giữ `vision_analyze` trong toolset + hint "closer look"): hỏi lại con mắt với prompt là CHÍNH câu hỏi, không cache. Ảnh chọn theo thứ tự mới nhất trước: batch lượt hiện tại (CHƯA vào DB - đọc history là hụt, vì thế ToolContext mang thêm `batch`) rồi tới history. Lỗi nào cũng trả thông báo trung thực cho model, không throw - cùng luật với web_search |
| Reactive fallback: lượt đính pixel bị provider từ chối 4xx -> dựng lại input describe/blind thử lại 1 lần + cache âm 10 phút | Đường chết còn lại sau auto-detect: endpoint ngoài 9Router không khai capability -> unknown -> đoán lạc quan là có vision -> model mù trả 400 -> trước đây chết lượt, người nhắn nhận "trục trặc kỹ thuật". Giờ bắt đúng chữ ký "4xx + lượt có part ảnh" (loại 401/403 sai key, 429 quota, 5xx tạm thời - SDK maxRetries đã lo), `markModelNoVision` ghi nhớ để các lượt sau đi thẳng describe/blind không đâm đầu lại. Anthropic không bao giờ bị đánh dấu (Claude đều có vision, 4xx ở đó là chuyện khác). Combo qua 9Router không đi đường này - router lột ảnh êm không lỗi, vì thế mới cần hybrid ở dòng trên |
| Model chính không vision: sidecar "đọc ảnh thuê" mô tả ảnh 1 lần, cache DB theo rel_path; không có sidecar thì bỏ ảnh + dặn bot nói thật | Mô hình text-mode + auxiliary.vision của Hermes (`agent/image_routing.py`), chọn mô-tả-lúc-nhận thay vì tool `read_image` kiểu GoClaw vì bot Zalo gần như luôn phải đọc ảnh người dùng gửi (vé số, hóa đơn) mà mỗi step tool là gửi lại cả hội thoại. Sidecar qua endpoint OpenAI-compatible - Gemini `v1beta/openai/` kiểm chứng 7/2026 còn sống, nhận base64 image_url; free tier `gemini-3.5-flash-lite` ~500 lượt/ngày (đo trên trang Rate Limit thật - thế hệ cũ 2.5-flash-lite đã bị Google RÚT serving, trả 404 dù vẫn trong /models; Gemma 4 quota khủng 14.4K/ngày nhưng rò `<thought>` thô vào output nên không dùng làm mắt) chỉ tốn khi có ảnh MỚI (cache vĩnh viễn tới khi dọn cùng nhịp `MEDIA_RETENTION_DAYS`). Prompt mô tả ép chép NGUYÊN VĂN chữ + số - mô tả chung chung vô dụng với việc dính tiền. Mô tả cả ảnh history sắp vào context nên ảnh passive-listen (gửi trước, @mention sau) vẫn đọc được |
| Bot TỰ DỰNG file .docx/.xlsx, model chỉ cấp nội dung có cấu trúc - KHÔNG chạy code model sinh ra | Đo thật: LLM API chỉ trả text, không có kênh file (cả DeepSeek trực tiếp lẫn qua 9Router, message chỉ có `role` + `content`). IDE code tạo được file vì IDE CHẠY code model viết ra - đường đó với bot Zalo là RCE qua prompt injection: bot đọc tin người lạ, không ai duyệt lệnh. Hermes có `code_execution_tool` + sandbox docker/modal, GoClaw có `credentialed_exec` + `exec_approval` - cả hai dựa trên tiền đề người dùng tin agent và ngồi ngay đó, không áp được. Tool ở đây chỉ nhận DỮ LIỆU qua schema Zod hẹp |
| Know-how định dạng nằm trong RENDERER, không nằm trong prompt | Skill `docx`/`xlsx` của Anthropic (GoClaw và Hermes đều adapt lại) chứa hàng trang quy ước - đó mới là thứ làm file "đẹp", không phải model giỏi. Nhét vào renderer thì chất lượng CỐ ĐỊNH kể cả chạy model rẻ, khác IDE nơi model tự viết code nên chất lượng dao động theo model. Gotcha đã kiểm chứng bằng đọc ngược XML: dual width DXA (PERCENTAGE vỡ ở Google Docs), ShadingType.CLEAR (SOLID render nền đen), bullet qua numbering config (chèn ký tự bullet thô thì Word không nhận là danh sách), tách xuống dòng thành nhiều Paragraph |
| .xlsx dùng `exceljs` dù nặng gấp 12 lần `write-excel-file`, và KHÔNG cài LibreOffice | File .xlsx lưu công thức ở `<f>` và kết quả đã tính ở `<v>`. Excel mở lên tự tính nên không cần `<v>`, nhưng MỌI công cụ xem trước (Zalo, Drive, Outlook) chỉ đọc `<v>` - thiếu là ô hiện TRỐNG. Đo: `write-excel-file` 0/3 ô có `<v>` và còn ghi thừa dấu bằng trong `<f>` (sai ECMA-376), `exceljs` 3/3. Skill Anthropic giải bằng chạy LibreOffice recalc; bot tự tính result được vì chính nó sinh ra dữ liệu, tiết kiệm ~1GB dependency + 1-3 giây mỗi lần gọi tiến trình con. Đổi lại chỉ hỗ trợ 2 phép bot tính được: nhân 2 cột, SUM một dải |
| Tool tạo file TỰ GỬI luôn, và câu trả về dặn model đừng gửi lại | Pattern `deliver` của GoClaw (`filesystem_write.go`): file tự động gửi kèm câu "do NOT send it again via message tool" + `DeliveredMedia.Mark()` chống trùng. Không dặn thì model rất dễ gọi tiếp `send_file` và người dùng nhận 2 lần. Tách 2 bước cũng tốn thêm một lượt gửi lại toàn bộ hội thoại (bài học token V2.5.4), và file nằm ở thư mục tạm mà `send_file` chỉ nhận shared-files hoặc URL |
| Đọc file OOXML trong test phải đi qua CENTRAL DIRECTORY | Có thư viện ghi zip kiểu streaming: local file header để `compSize = 0` (flag bit 3, kích thước thật nằm ở data descriptor SAU dữ liệu). Đọc theo local header ra dữ liệu RỖNG - và test dựa vào đó sẽ XANH một cách vô nghĩa vì không có gì để so. `read-zip-entry.ts` tự viết bằng node:zlib, không thêm dependency |
| Zalo không chặn .docx/.xlsx (đã đo, không suy đoán) | `zca-js` kiểm `restricted_ext_file` - danh sách CẤM do server Zalo trả về lúc login, chạy ở client trước khi chạm mạng. Thử thật bằng session bot (chỉ upload, không gửi tin cho ai): `.exe` bị chặn, `.docx`/`.xlsx` qua được. Nên không phải đổi hướng sang PDF |
| Tool `create_image` XIN SSE bằng header `Accept: text/event-stream`, KHÔNG dùng `?response_format=binary` dù binary nhẹ hơn 15 lần | Router nằm sau Cloudflare, mà Cloudflare cắt kết nối bằng HTTP 524 khi origin im lặng quá lâu. Đường không stream chỉ trả byte đầu tiên LÚC VẼ XONG nên vẽ lâu là chết. Đo cùng một prompt chạy song song: binary byte đầu sau **125.4s -> HTTP 524**, SSE byte đầu sau **1.8s -> HTTP 200**, xong ở giây 62.7. Đã dính thật trên Zalo: 2 lần gọi liên tiếp đều 524, người dùng không nhận được ảnh nào. Đổi lại tốn ~2.5 lần băng thông (504 KB so với 199 KB - base64 cộng event tiến độ) - đáng, vì đường kia có lúc không ra được ảnh. Client vẫn đọc được JSON thường và bytes thô: chỉ provider codex mới stream, endpoint khác nhận header rồi trả JSON như cũ. `IMAGE_GEN_TIMEOUT_MS` KHÔNG cứu được ca này vì Cloudflare cắt trước |
| Thang cỡ chữ chỉ dùng SỐ NGUYÊN, và chữ nhỏ phải giãn dòng >= 1.5 | Đo trên dashboard thật: 13 cỡ chữ khác nhau, nhiều cặp cách nhau 0.5px (10/10.5, 11/11.5, 12/12.5, 13/13.5). Mắt không phân biệt nổi nửa pixel nên chúng không tạo thứ bậc gì, chỉ làm mọi thứ trông lệch nhẹ khắp nơi. Gộp về 11/12/13/14/15/17. Về giãn dòng: chữ 12px đang để `text-xs` của Tailwind (16px, tỉ lệ 1.33) - quá chật cho TIẾNG VIỆT vì dấu xếp chồng lên nhau (ế, ộ, ữ, ằ) nên cần rộng hơn tiếng Anh chứ không phải hẹp hơn. Font Be Vietnam Pro giữ nguyên: đã kiểm tra nó tải đúng đủ 4 weight, và nó vốn thiết kế cho tiếng Việt |
| Modal settings phân vùng bằng TƯƠNG PHẢN NỀN (vùng xám nhạt, ô nhập trắng nổi lên), ba cấp bậc khác nhau về KIỂU chứ không phải về cỡ | User phản hồi "rối mắt" qua BA vòng, mỗi vòng lộ một tầng nguyên nhân. (1) Cỡ chữ: 13 cỡ lệch nhau 0.5px - sửa xong vẫn rối. (2) Cấu trúc: khung lồng khung (8 viền cho 4 ô nhập) và tiêu đề bậc 14px/500 đứng cạnh nhãn ô 13px/500 - chênh 1px cùng độ đậm, mắt không biết cái nào chứa cái nào; đổi tiêu đề bậc thành nhãn nhóm (11px VIẾT HOA giãn chữ màu nhạt), bỏ viền bậc, ngăn bằng đường kẻ mảnh - user VẪN "chưa thấy phân cách". (3) Đường kẻ 1px màu #e4e8ef trên nền trắng là quá mờ: đổi sang mỗi bậc một vùng `bg-tile` không viền - tương phản nền tách vùng rõ tức thì mà không thêm nét nào, ô nhập trắng cũng tự nổi lên. Bài học: phân cách phải đủ ĐẬM để người không chủ tâm tìm vẫn thấy; đường kẻ mảnh chỉ đẹp với người đã biết nó ở đó. Kèm theo: chú thích cắt xuống mức một dòng, và tránh ký hiệu `~` trước số ở cỡ chữ nhỏ (render nhìn thành dấu trừ, "~1 phút" đọc ra "-1 phút") - viết chữ "khoảng" |
| Ô nhập trong modal BẮT BUỘC có nhãn, không dựa vào placeholder | Ba ô trong modal đọc ảnh chỉ có placeholder: điền xong là placeholder biến mất, không còn gì cho biết ô nào là Base URL, ô nào là Model. Modal vẽ ảnh lại có nhãn đầy đủ - hai modal cạnh nhau hai kiểu. Gom vào `ModalField` dùng chung (nhãn + `htmlFor` + chú thích tùy chọn). Chú thích chỉ dùng khi nói điều KHÔNG suy ra được từ nhãn: đo trên modal cũ chú thích chiếm 61-69% số chữ, phần lớn là nhắc lại thứ nhãn hoặc subtitle đã nói - `description` của ChainStep thành tùy chọn vì subtitle modal đã mô tả nguyên chuỗi rồi |
| Log ghi ra FILE xoay vòng (pino-roll), không lưu SQLite như trace | Trước đó log chỉ ra stdout: đóng terminal là mất sạch, trên VPS không còn gì để lần lại. Chọn file chứ không SQLite KHÔNG phải vì hiệu năng (bot cá nhân vài tài khoản, volume rất thấp, SQLite thừa sức) mà vì ĐỘ BỀN của chính đường ghi log - log là thứ tuyệt đối không được hỏng, vì lúc cần nó nhất là lúc mọi thứ khác đang hỏng; ghi file không phụ thuộc DB. Hai target hai mức: terminal theo `LOG_LEVEL` (gọn để đọc lướt), file ở `trace` nhận hết kể cả debug (đủ, vì lúc cần tới là sự cố đã xảy ra, không quay ngược bật debug được). Mức GỐC của logger phải hạ xuống `trace` - để nguyên `info` thì dòng debug bị chặn ngay từ đầu và target file không bao giờ nhận được |
| Trace từng step agent lưu vào SQLite + xem trên dashboard, KHÔNG đẩy sang dịch vụ trace ngoài | Bản đầu chỉ log `toolResults` nên bot trả lời sai là phải suy luận ngược: vụ dò vé số "toàn bộ chẩn đoán phải đi đường vòng qua DB + tái hiện tay", vụ `imageIndex` phải đoán từ câu lỗi. Giờ mỗi step ghi đủ: model nói gì, gọi tool nào với THAM SỐ gì, `warnings` (chỗ nhà cung cấp báo tham số bị âm thầm bỏ qua - đúng loại lỗi đã dính 2 lần), usage riêng từng step. Chọn SQLite thay vì Langfuse/OTel không phải vì chúng kém: trace chứa NGUYÊN VĂN tin nhắn của người thật, đẩy lên SaaS là đẩy hội thoại của họ ra khỏi máy - quyết định về quyền riêng tư chứ không phải kỹ thuật; còn tự nuôi Langfuse (cần cả Postgres lẫn ClickHouse) thì nặng hơn cả con bot. Bảng `agent_steps` phình nhanh nhất DB nên dọn cùng nhịp media |
| `reasoning` (model suy nghĩ) CÓ hay KHÔNG là do MODEL, không phải do code | Đo thật qua router: DeepSeek trả nguyên chuỗi suy nghĩ kể cả khi không stream (`reasoning_content` + `reasoning_tokens`); OpenAI `gpt-5.6-sol` không trả gì khi không stream, có stream cũng chỉ một dòng nhãn tóm tắt kiểu "**Explaining simple arithmetic steps**" - họ cố tình không phơi chain-of-thought. `@ai-sdk/openai-compatible` đã tự map `reasoning_content` thành reasoning part nên code chỉ việc đọc `step.reasoningText`. Nghĩa là muốn soi model nghĩ gì thì phải đổi sang DeepSeek, không phải sửa code |
| `cx/gpt-5.5-image` KHÔNG phải model vẽ ảnh - nó là GPT-5.5 kèm tool `image_generation`, và nó TỰ THIẾT KẾ | Đọc `imageProviders/codex.js` hàm buildBody: router bóc đuôi `-image` (`stripImageSuffix`) rồi gọi model chat với `instructions: ""`, `input: [{role:"user", content: <prompt>}]`, `tools:[image_generation]`, `tool_choice:"auto"`. Nghĩa là trong đường ống có HAI model, và cái thứ hai đọc prompt như tin nhắn người dùng rồi tự quyết bố cục/màu/nội dung. Stream cũng lộ ra điều này: có `response.output_item.done` TRƯỚC lúc vẽ và `response.output_text.delta` sau khi vẽ. Hệ quả trực tiếp ở dòng dưới |
| Mô tả tool CHỈ nói điều có lý do CHỨC NĂNG, tuyệt đối không kê đơn thẩm mỹ | Đã từng nhét vào mô tả tool một "brief thiết kế 5 mục" kèm ví dụ cụ thể (chữ bên trái, minh họa bên phải, nền navy chuyển tím than, điểm nhấn cyan). Model chép y ví dụ nên MỌI ảnh ra một khuôn - và còn xấu hơn trước. Đo đối chứng: gửi prompt TRẦN TRỤI (đúng câu người dùng gõ, không gợi ý gì) 3 lần cho ra 3 thiết kế khác hẳn nhau, đều dày dặn chuyên nghiệp, GPT-5.5 còn tự nghĩ thêm cụm icon kèm chữ tiếng Việt mà không ai bảo. Brief chỉ trói tay nó. Giữ lại đúng 3 thứ có lý do chức năng: chữ người dùng phải chép nguyên văn (không thì ảnh ra chữ bịa), tỉ lệ phải nói trong prompt (API không có tham số kích thước), và trung thành với ý người dùng - họ không nói phong cách thì ĐỪNG tự bịa ràng buộc. Bài học chung: gu thẩm mỹ của người viết code lọt vào mô tả tool là mọi ảnh của mọi người dùng đều dính |
| Gửi `quality` cho tool vẽ (mặc định `high`), không để provider tự chọn | Tham số này đi thẳng vào cấu hình `image_generation` của provider. Không gửi thì nó dùng mức thấp hơn. Đo A/B cùng prompt: `high` ra ảnh giàu chi tiết hơn hẳn (khối phát sáng, lớp sóng hạt, nhiều tầng biểu đồ) mà KHÔNG chậm hơn (50s so với 60s). Đây là tham số ĐỔI TIỀN LẤY CHẤT LƯỢNG - nhà cung cấp thường tính phí cao hơn ở mức này, nên để chỉnh được bằng `IMAGE_GEN_QUALITY`. Lưu ý dễ nhầm: dropdown "Instant/Medium/High" trên ChatGPT web KHÔNG phải cái này - đó là mức suy nghĩ của model CHAT (tương đương `LLM_REASONING_EFFORT`), không chạm tới tool vẽ |
| Prompt vẽ ảnh phải CHÉP NGUYÊN VĂN chữ người dùng đưa, không tóm tắt thành chủ đề | Mô tả tool bản đầu chỉ dạy tả phong cách (chủ thể, ánh sáng, tông màu) nên với yêu cầu e-magazine từ một bài viết, model rút gọn cả bài thành "Chủ đề thiền Phật giáo tại TP. Hồ Chí Minh" - ảnh ra sẽ toàn chữ bịa thay vì chữ người dùng cần. Giờ mô tả bắt đặt từng phần trong ngoặc kép kèm vai trò (TIÊU ĐỀ / ĐOẠN MỞ / TRÍCH DẪN). Kiểm chứng: prompt 700 ký tự chép nguyên văn cho ra trang e-magazine đúng từng câu, đủ dấu tiếng Việt. Log thêm `promptChars` vì nhìn 200 ký tự đầu không phân biệt được "chép nguyên văn" với "tóm tắt" |
| Ảnh gốc để SỬA nằm ở trường `image` (data URI), không phải `ref_image`; và KHÔNG gửi `size` | Tên trường lấy từ source 9Router (`imageProviders/codex.js` đọc `body.image`/`body.images`) rồi kiểm chứng bằng gọi thật - sai tên thì provider bỏ qua âm thầm, vẫn ra ảnh nhưng là ảnh VẼ MỚI chứ không phải bản sửa. Sửa ảnh là sửa thật: gửi ảnh mèo + "đổi mũ thành beret đỏ" thì mũ đổi còn mèo/sách/đèn giữ nguyên từng pixel. `size` thì ngược lại - model BỎ QUA: xin `1024x1024` hai lần nhận về `1024x1536` rồi `1536x1024`, nên không gửi và không hứa với người dùng là chỉnh được kích thước (muốn dọc/ngang phải nói trong prompt) |
| Endpoint vẽ ảnh trả lỗi theo HAI hình dạng khác nhau - parser phải chịu được cả hai | Đo thật: `400` trả `{"error":{"message":"..."}}` (error là OBJECT) còn `401` trả `{"error":"API key required..."}` (error là CHUỖI). Parser chỉ đọc `error.message` sẽ hiện `undefined` cho người dùng đúng lúc họ cần biết nhất là sai key. Có test hồi quy khẳng định câu lỗi không chứa `undefined` |
| `create_image` nhắn "đang vẽ..." TRƯỚC khi gọi provider, và chỉ nhắn sau khi đã qua cửa kiểm trần | Đo thật 4 lần: 63-69 giây mỗi ảnh. Im lặng ngần ấy thời gian thì người nhắn tưởng bot lơ rồi nhắn lại - vừa khó chịu vừa đẻ thêm một lượt agent. Nhưng nhắn trước khi biết có vẽ được không là hứa lèo, nên thứ tự bắt buộc là: kiểm cấu hình -> nạp ảnh gốc -> kiểm trần -> nhắn -> vẽ. Test khẳng định đúng thứ tự này. Chỉ báo "đang nhập" không thay được: nó tự tắt sau 2 phút và không nói được lý do chờ |
| Trần vẽ ảnh tách riêng khỏi trần tạo file, phần đếm dùng chung `shared/hourly-rate-limit` | Hai tool khác hẳn về giá: dựng .docx chỉ tốn CPU của mình, còn mỗi ảnh là một lần TRẢ TIỀN cho nhà cung cấp - trần ảnh là trần tiền chứ không phải trần CPU. Nhưng phần đếm thì giống hệt và phần khó không phải chuyện đếm mà là chuyện DỌN (giữ entry cho mọi thread từng nhắn là rò rỉ bộ nhớ - hồi quy V2.4), nên tách factory dùng chung thay vì chép: bản sửa cho chỗ này sẽ không bao giờ tới chỗ kia. Lỗi provider VẪN tính vào trần, không thì spam retry đốt quota thoải mái |
| Model vẽ ảnh phải gõ tay, không tự dò được như sidecar đọc ảnh | `/v1/models` của 9Router trả 22 model chat và KHÔNG model nào có `imageOutput` - model vẽ ảnh nằm ở registry Media Providers riêng. Nên không copy được mẹo auto-detect capability của vision. Bù lại bằng nút "Vẽ thử 1 ảnh" trong modal: khác nút Test của sidecar (ảnh 1x1, gần như miễn phí), nút này vẽ thật nên mất ~1 phút và tính phí - nhưng đó là cách DUY NHẤT chứng minh tên model đúng |
| Lượt agent MỞ row `agent_turns` trước khi chạy, chốt usage sau | Ghi row sau khi lượt xong thì lượt NÉM LỖI không có id: trace của các step đã chạy bị vứt (chạy tốt 5 step rồi step 6 gặp 500 là mất sạch manh mối), và log trong lượt không mang nổi correlation id nên hai lượt liên tiếp cùng thread chỉ phân biệt được bằng timestamp. Đo trên bản sao DB thật: schema nhận INSERT 2 cột (mọi cột số DEFAULT 0), giá 2.1ms INSERT + 1.9ms UPDATE cho lượt dài hàng chục giây. Row mồ côi khi process bị kill KHÔNG lên trang Trace vì `recentAllStmt` vốn đã `JOIN agent_steps` - hàng phòng thủ có sẵn, không phải viết thêm. Đổi lại Overview đếm thừa 1 lượt 0 token mỗi lần bị kill |
| Mảng `trace` do CALLER sở hữu, truyền VÀO `runAgentTurn` | Trả trace về ở cuối hàm nghĩa là đường ném lỗi làm nó rơi theo stack - mất đúng lúc cần nhất. Truyền vào thì `catch` bên ngoài còn đủ những step đã chạy. Chọn cách này thay vì nhét trace vào error object: không phải bịa thuộc tính lên Error, và test được bằng cách đọc thẳng mảng của mình |
| Seam `resolveModel` để test tiêm model giả | Không có seam thì mọi nhánh của vòng lặp - retry glitch, bỏ pixel khi 4xx, lượt chốt khi chạm trần - đều phải gọi provider thật, tức là không bao giờ được test. Cùng nếp đã có với `persistBatchImages` (tiêm downloader) và `web-search-providers` (tiêm fetch). Lưu ý khi viết mock: `usage` của `LanguageModelV4` là cấu trúc LỒNG (`inputTokens.total`), không phải ba số phẳng như `result.totalUsage` mà agent-loop đọc - dựng sai tầng thì mọi số về 0 và lượt hợp lệ bị chấm nhầm là glitch router |
| Luật dùng tool nằm ở `persona-tool-rules.ts`, gate theo tool ĐANG BẬT | Persona nền dạy luật cứng thì tắt "Vẽ ảnh AI" trên dashboard vẫn kéo theo 4 dòng dạy viết prompt vẽ ảnh - đo được 1163 ký tự mỗi lượt, lại mâu thuẫn với chính prompt (bảo bot đừng hứa việc ngoài danh sách rồi dạy nó cách làm đúng việc đó). Theo `hermes-agent/agent/system_prompt.py` ("only inject when the tools are loaded"): chữ luật ở module prompt riêng, gate bằng tên tool lúc ghép. KHÔNG gắn vào `tool-registry.ts` - registry còn phục vụ API dashboard, đẩy chữ prompt sang trình duyệt là thừa. Đo trên bản sao cấu hình THẬT (env test cho số đẹp hơn thực tế vì chưa cấu hình endpoint vẽ ảnh): 3186 ký tự chuyển từ luôn-có-mặt sang có-điều-kiện. Account đang bật đủ 12/12 tool nên prompt vẫn 6600 - chưa lời token nào, mới chỉ đúng đắn. Tắt Vẽ ảnh AI thì còn 5566, tắt hết tool còn 2204 |
| `stripHtmlTags` ở `html-to-text.ts`, không ở `web-search-providers.ts` | Module bóc chữ (thuần) đi mượn hàm của module gọi mạng (có logger) là cạnh phụ thuộc ngược chiều, và logger đọc `DATA_DIR` lúc nạp module - đủ để test của một file thuần đẻ ra file log trong `data/` production. Sửa gốc bằng cách trả hàm về đúng nhà thay vì thêm `setupTestEnv` cho test |
| `turnId` bám log bằng AsyncLocalStorage + `mixin` của pino, không bằng child logger | Child logger chỉ phủ module nào cầm đúng cái logger đó. Đo trên lượt thật (turn 65): 11 dòng có turnId, 3 dòng KHÔNG - chúng của `web-fetch-tool` vốn tự tạo logger riêng, và còn 5 module như vậy. Luồn `turnId` qua chữ ký hàm thì `searchWeb(query, opts)`/`fetchViaJina(url)` phải nhận thêm tham số chẳng liên quan gì tới chúng. ALS giữ ngữ cảnh qua await nên `logger.ts` chỉ cần đọc trong `mixin` là mọi dòng tự có, kể cả tool thêm mới sau này. Test khẳng định hai lượt chạy xen kẽ không lẫn id của nhau - đúng ca mà biến module sẽ sai. BẪY: `mixin` PHẢI trả BẢN SAO mới mỗi lần - `defaultMixinMergeStrategy` của pino (`lib/proto.js`) làm `Object.assign(mixinObject, mergeObject)`, tức ghi đè vào chính object mình trả về, nên trả thẳng object trong ALS thì mỗi dòng log lại nhét trường của nó vào đó vĩnh viễn. Đã lọt ra lượt thật: một dòng `web-fetch` phình lên 28 trường mang theo cả `text`/`reasoning`/`toolResults` của dòng trước - vừa rác vừa rò nội dung tin nhắn sang dòng chẳng liên quan, mà `readRecentLogs` chỉ đọc 4MB cuối nên dòng phình ăn mất cửa sổ đọc |
| Cột `agent_steps.attempt` | Lượt retry (router rỗng) và lượt dựng lại không kèm pixel đều đánh số step lại từ 1, nên trace một lượt ra 1,2,3 rồi 1,2,3; xếp theo `step_number` đọc thành 1,1,2,2,3,3 và trông y hệt model lặp vô hạn. ALTER mặc định 1 nên dòng cũ vẫn hợp lệ. Badge trên UI chỉ hiện khi > 1 - dán "lần 1" lên mọi step chỉ làm nhiễu |
| `cachedTokens = 0` KHÔNG kết luận được gì (sửa lại nhận định cũ) | Ghi chép cũ nói "executor cx/ không trích usage" - đọc source thì NGƯỢC LẠI: `translator/response/openai-responses.js:472` CÓ đọc `input_tokens_details.cached_tokens`. Chỗ nhầm là lấy kết luận đúng về `reasoningTokens` (dòng 474 quả thật không truyền nó) rồi suy rộng sang cache. Sự thật ở `translator/concerns/usage.js:5`: `buildUsage` chỉ thêm `prompt_tokens_details` khi > 0, nên "upstream báo 0 lần trúng" và "upstream không báo trường này" về tới client giống hệt nhau. `> 0` là bằng chứng chắc chắn cache trúng; `= 0` phải soi dashboard 9Router mới biết |
| Tool `schedule_task` KHÔNG nhận chuỗi datetime, chỉ nhận `{date,time}` rời hoặc `{inMinutes}` | Chuỗi naive kiểu `"2026-08-01T15:00"` qua `new Date()` bị Node hiểu theo giờ HỆ ĐIỀU HÀNH - trên VPS đặt UTC thì "3h chiều" thành 22:00 giờ VN. Hermes dính đúng ca này (`jobs.py`, issue #51021), phải vá bằng cách neo timestamp naive vào timezone cấu hình thay vì timezone máy chủ. Chặn tận gốc bằng schema: quy đổi CHỈ đi qua `zonedWallClockToUtc(date, time, tz)` (luxon), không còn đường nào để cách LLM viết chuỗi làm đổi ý nghĩa giờ |
| `luxon` là dependency TRỰC TIẾP trong `package.json`, không tự cộng trừ offset tay | `cron-parser@5.6.2` (cần cho lịch `cron`) đã kéo `luxon@^3.7.2` vào cây phụ thuộc, nhưng pnpm layout chặt không cho import dependency GIÁN TIẾP (dự án từng dính đúng lỗi này khi thử import `@ai-sdk/provider` để lấy kiểu) nên phải khai thẳng mới dùng được. VN không có DST nên tự tính offset "trông có vẻ dễ", nhưng `BOT_TIMEZONE` là cấu hình - đặt `Europe/Paris` là gặp DST ngay; test hồi quy của `schedule-parser`/`next-run` chạy cả `TZ=UTC` lẫn `TZ=America/New_York` phải ra cùng kết quả |
| Job `once` trễ quá grace vẫn GỬI, kèm tiền tố "(nhắc trễ, lịch gốc HH:MM)" | Hermes vứt hẳn one-shot quá hạn (`_recoverable_oneshot_run_at` trả `None`), goclaw tắt job im lặng - cả hai làm lời nhắc biến mất không dấu vết. Với bot cá nhân, nhắc hẹn bị nuốt im lặng là kết cục tệ nhất: user đã hẹn thì phải biết nó đã xảy ra, kể cả biết muộn. Nhãn tính độ trễ từ cột `run_at` (mốc GỐC, không bao giờ bị ghi đè) chứ không phải `next_run_at` (có thể đã bị trần ngày đẩy hoặc gửi-hỏng phục hồi) - dùng nhầm cột làm nhãn trễ biến mất ở đúng job từng bị hoãn, tức đúng job cần nhãn đó nhất |
| Không retry ở tầng scheduler | `agent-loop` đã có 2 tầng chống chịu (retry glitch router khi completion rỗng, `maxRetries: 2` của SDK) - thêm tầng thứ ba ở scheduler là ba tầng chồng nhau, một job hỏng có thể nhân lên tới 6 lần gọi provider. Job lỗi thì ghi run `error`, đợi lượt sau theo lịch. Riêng lỗi ở ĐƯỜNG GỬI (mất mạng thoáng qua, khác lỗi provider) có thử lại tối đa 3 lần qua cột bền `delivery_attempts` - đây là retry cho tầng vận chuyển tin, không phải retry lượt LLM |
| 9 tool bị loại khỏi lượt theo lịch (`runsInScheduledTurn: false`), hai nhóm lý do khác nhau | Nhóm 1 - thiếu hạ tầng cho lượt cô lập: `add_reaction` (không có `msgId` thật để thả vào), `read_image` (lượt này không có ảnh nào), `schedule_task` (luật số 1 của Hermes: job không được đẻ job), `save_memory` (không repo tham khảo nào loại tool này, nhưng đường nội dung web - job `agent` đọc web rồi tự quyết ghi gì - đi vào trí nhớ VĨNH VIỄN là prompt injection thật, và lượt theo lịch cũng không có phát ngôn nào của user để mà "học"). Nhóm 2 - phát hiện ở vòng review toàn nhánh: `send_file`, `create_word_document`, `create_excel_file`, `create_image`, `tag_member` đều gọi thẳng `enqueueSend` (rate-limiter THEO THREAD của `middleware/rate-limiter.ts`) để gửi, KHÔNG đi qua `deliverProactively`/`reserveProactiveSlot` của scheduler - bộ đếm trần `SCHEDULER_MAX_PROACTIVE_PER_DAY` chỉ tăng ở đường đó nên 5 tool này né hoàn toàn trần, kể cả nhánh `[SILENT]` (không markRun). Với `LLM_MAX_STEPS` mặc định, một lượt agent theo lịch có thể gọi tới 8 lần các tool này mà không tin nào bị đếm hay giãn cách - trên tài khoản có thể bị khoá vì hành vi giống spam thì đây là đường không được phép ship. Chọn loại (an toàn, rẻ) thay vì cho đi qua đường đếm (đợi tool đó tự đi đúng đường `deliverProactively` ở đợt sau) - báo cáo dạng CHỮ (`kind='message'`/`agent` trả text) vẫn là use case chính, không bị ảnh hưởng |
| `getDailyUsage` gom theo ngày trong JS (`dayKeyOf` qua luxon) thay vì SQL `datetime(created_at, '+7 hours')` | Cộng cứng 7 tiếng trong SQL chỉ đúng với `Asia/Ho_Chi_Minh` không DST - sai ngay khi `BOT_TIMEZONE` là zone có DST, mà đây là biến cấu hình được chứ không phải hằng số. Câu SQL giữ nguyên mệnh đề `created_at >= ?` để chặn phạm vi quét, việc gom theo ngày chuyển hẳn sang JS. Chi phí chấp nhận được: đo trên DB thật chỉ 67 dòng `agent_turns` (179 `messages`) tổng cộng - cửa sổ 7 ngày luôn là vài chục dòng. Cùng gốc bệnh với 2 chỗ khác đang tính "hôm nay" theo ngày UTC (`overview-stats.ts`, frontend `overview-page.tsx`) nên sửa CẢ BA cùng lúc - hai bên trước đó cùng sai theo cùng một kiểu nên tình cờ khớp nhau, sửa lẻ một chỗ sẽ làm lộ ra chỗ kia |
| Trần tin chủ động đếm theo TIN ZALO (không phải lượt job), lưu ở bảng bền `proactive_send_counters` (không phải bộ nhớ tiến trình) | `sendReplyInParts` cắt câu trả lời dài thành tới `ZALO_MAX_MESSAGE_PARTS` (5) tin - đếm theo LƯỢT job thì "trần 10 tin/ngày" thật ra cho phép tới 50 tin Zalo thật. Và trần ngày là ràng buộc DUY NHẤT ở chiều KHỐI LƯỢNG (lưới đỡ đầu ở `SCHEDULER_MIN_INTERVAL_MINUTES` chỉ chặn TẦN SUẤT - `every 5 phút` vẫn lọt ra 288 lượt/ngày); đếm trong bộ nhớ thì restart bot là mất trần. KHÔNG đếm từ `scheduled_job_runs`: bảng đó bị `openRun` tự prune xuống `SCHEDULER_RUN_LOG_KEEP` (mặc định 50) dòng MỖI JOB - 1 job `every 5 phút` đẻ 288 dòng/ngày thì dòng buổi sáng bị xoá trong vài giờ, đếm lại tụt về 0 giữa ngày (undercounting, sai chiều nguy hiểm nhất cho một cơ chế chống spam). Giành chỗ NGUYÊN TỬ bằng SQL `INSERT ... ON CONFLICT DO UPDATE SET count = count + 1 WHERE count < ?` (đọc `changes` để biết có giành được không) - cần thiết vì bỏ khoá thread ở tầng tick (để tin thật không bị chặn theo job) làm N job cùng thread cùng đến hạn có thể cùng đọc-rồi-ghi trúng cùng một giá trị đếm cũ |
| Job bị guard chặn (account rớt phiên / thread tắt bot / trần ngày) KHÔNG tiêu suất chạy; job `once` gửi hỏng thử lại tối đa 3 lần qua cột bền `delivery_attempts` | Bản đầu gọi `markRun` cho MỌI status kể cả `skipped` - job `once` (`maxRuns` luôn = 1) chạm max ngay ở LẦN BỊ CHẶN ĐẦU TIÊN, `enabled=0` vĩnh viễn dù chưa từng gửi được gì. Account rớt phiên là ca BÌNH THƯỜNG của zca-js, không phải hiếm. Bất biến chốt: "một lời nhắc chưa từng gửi được thì không bao giờ được coi là đã chạy". Gửi hỏng (mất mạng thoáng qua) thì thử lại tối đa 3 lần trước khi tiêu suất - phân biệt "chưa gửi" (retry) với "gửi được MỘT PHẦN rồi hỏng" (không retry, coi là đã chạy - người dùng đã nhận chữ, gửi lại từ đầu là nhắn trùng) bằng cách kiểm `reply.deliveredText` khác rỗng trước khi rẽ nhánh |
| "Chạy thử ngay" trên dashboard phải GIÀNH job (`next_run_at = NULL`) trước khi dispatch, đúng bài clear-before-dispatch của vòng tick thật - CỘNG THÊM route từ chối 409 khi job còn lượt `status='running'` | Nếu job đang QUÁ HẠN lúc bấm thử (đúng use-case có giá trị nhất) và job `kind='agent'` chạy lâu hơn `SCHEDULER_TICK_MS`, một tick THẬT có thể xen vào giữa. `listDueJobs()` lọc `next_run_at IS NOT NULL` nên giành job đóng cửa sổ đụng độ với TICK TƯƠNG LAI hoàn toàn (tick sau đó không còn thấy job "due"), nhưng KHÔNG đóng được cửa sổ với lượt ĐÃ dispatch TRƯỚC lúc bấm thử - claim không huỷ hay biết gì về một lượt đang chạy dở từ trước (vòng review toàn nhánh phát hiện tài liệu từng khẳng định nhầm "đóng hoàn toàn"). Ca đó: tick thật đã dispatch, đang chạy (row `scheduled_job_runs` còn `running`), user bấm thử - snapshot/claim/restore của trial không hề hay biết, dẫn tới nhắn trùng, mất kết quả lượt thật, hoặc job chết im lặng khi 2 tab cùng bấm. Vá thêm: route `POST /:id/run` từ chối 409 NGAY khi thấy job còn dòng `status='running'`, chặn TỪ TRƯỚC lúc trial kịp snapshot - chỉ còn 1 khe TOCTOU cỡ 1 câu SQL, đủ hẹp để loại ca thật (lượt agent dài phút). Restore đặt trong `try/finally` để job không kẹt vĩnh viễn ở trạng thái đã giành nếu có lỗi bất ngờ |
| Nhánh HỎNG của tool trả object `{ok:false,loi}` (`ketQuaLoi`), nhánh thành công vẫn trả chuỗi trần | Repo đã chốt "tool không ném ra agent loop" - mọi nhánh hỏng bắt lại rồi trả câu cho model. Hệ quả không ai để ý: AI SDK CHỈ sinh phần `tool-error` khi `execute` NÉM, nên nhánh hỏng trả chuỗi đi vào `toolResults` như kết quả bình thường và `tool-loop-guard` mù với nó - hai trong ba bộ đếm của guard là code chết từ lúc viết ra (đo thật: `web_fetch` hỏng trên 8 URL khác nhau, `web_search` rỗng trên 8 từ khóa khác nhau, guard đều không chặn). Chọn OBJECT chứ không phải tiền tố chuỗi kiểu `"[LỖI] ..."` vì nội dung web và tin nhắn của người lạ đi thẳng vào chuỗi kết quả: luật nào dựa trên CHỮ cũng có thể bị soạn cho trùng, còn hình dạng object thì nội dung không tự giả được (nó luôn nằm trong TRƯỜNG, không bao giờ là cái vỏ). `laKetQuaLoi` kiểm cả `typeof loi === "string"` vì repo có sẵn 4 type khác cùng hình dạng `{ok:false,...}` nhưng khác tên trường (`reason`, `error`). Kết quả RỖNG: rỗng mà chắc chắn là thành công (`schedule_task list`), rỗng mà không phân biệt được với hỏng là hỏng (`web_search` - chuỗi provider nuốt lỗi rồi cũng trả mảng rỗng) |
| Lớp làm sạch đầu ra (`zalo/sanitize-reply-text.ts`) đặt ở tầng CALLER, không đặt trong `sendReplyInParts` | `sendReplyInParts` phục vụ cả lượt chat lẫn lượt theo lịch, mà hai đường có luật khác nhau: lượt theo lịch phải kiểm `[SILENT]` TRƯỚC khi làm sạch (làm sạch trước thì bộ lọc bỏ mất chính cái nhãn mà nhánh im lặng dựa vào, và job "không có gì mới" sẽ nhắn mỗi sáng đúng thứ nó sinh ra để tránh), còn job hỏng thì IM chứ không nhắn câu lỗi kỹ thuật. Nội dung chặn: markdown (Zalo không render nên `**đậm**` hiện nguyên ký tự), rò system prompt (chặn HẲN - gửi nửa vời thì không ai biết phần nào còn rò), nhãn `[SILENT]` lọt vào chat thường. Dấu hiệu rò prompt lấy từ hằng số dùng chung `agent/prompt-leak-markers.ts` mà `persona-prompt.ts` nội suy vào - sửa lời persona mà quên sửa bộ canh thì lớp chặn im lặng ngừng hoạt động. Kiểm rò trên chữ GỐC trước khi bỏ định dạng, để `**Quy tắc an toàn...**` không lách được. CỐ Ý bỏ qua `__đậm__`/`_nghiêng_` (gạch dưới sống đầy trong tên file và URL thật) và chỉ bỏ DÒNG KẺ của bảng markdown chứ không dựng lại bảng - dòng kẻ không mang chữ nào nên bỏ là mất trắng, còn dựng lại bảng thành văn xuôi là đoán mò |
| Ngưỡng guard bị kẹp xuống dưới `LLM_MAX_STEPS` lúc dựng, không hạ mặc định trong env | Ba số 5/8/5 bê từ `tool_guardrails.py` của Hermes nhưng bê thiếu ngữ cảnh: ở Hermes `max_iterations` mặc định là 90 nên 8 chỉ là 9% ngân sách, còn ở đây trần mặc định là 8 nên ngưỡng 8 đứng đúng bằng trần - mỗi step một lệnh gọi thì `stepCountIs` luôn dừng trước, bộ đếm không bao giờ chạm tới. Kẹp lúc dựng (`nguongTheoTranStep`) chứ không hạ mặc định để người đặt `LLM_MAX_STEPS=30` vẫn được đúng ba số của Hermes. Nói cho đúng mức lợi: với trần 8 thì kẹp chỉ đưa `chanCungToolLoi` từ 8 xuống 7, sớm hơn ĐÚNG một step - giá trị thật của guard nằm ở chỗ nó đếm được nhánh hỏng không-ném, kẹp chỉ để ngưỡng không nằm ngoài tầm với |

## Repo tham khảo

Clone shallow (chỉ đọc, không build, không sửa) tại `D:\source-code\zalo-agent-references\` - đặt ngoài project để không dính vào git/pnpm/tsc:

| Repo | Dùng để tra |
|---|---|
| [zca-js](https://github.com/RFS-ADRENO/zca-js) | Source lib chính: `src/apis/` (chữ ký hàm thật), `examples/login.ts`, `examples/echobot.ts` |
| [zalo-agent-cli](https://github.com/PhucMPham/zalo-agent-cli) | Pattern QR login, lưu credential mã hóa. **Không harvest phần bank transfer/VietQR** |
| [zalo-personal](https://github.com/caochitam/zalo-personal) | Catalog 141 action - cách gọi zca-js cho từng tính năng |
| [deplao-builder](https://github.com/babyvibe/deplao-builder) | Pattern quản lý nhiều instance zca-js cùng lúc |
| [goclaw](https://github.com/nextlevelbuilder/goclaw) | Pattern tool hệ thống: datetime tool (weekday server tính), chuỗi web search Brave -> DDG, UI Built-in Tools. **License CC BY-NC 4.0 - chỉ soi pattern, KHÔNG copy code** |
| [hermes-agent](https://github.com/NousResearch/hermes-agent) | Agent hot nhất 2026 (Nous Research, MIT): toolset theo kênh, webhook-safe toolset (chống prompt injection), cascade provider web search trả phí -> free |

Bản `zca-js` trong `references/` chỉ là sách tra cứu - runtime dùng bản cài trong `node_modules` qua pnpm.

## Ràng buộc từ Zalo (qua zca-js)

- 1 listener/account: mở Zalo Web trên trình duyệt sẽ đá listener bot (bot tự reconnect với backoff).
- Rủi ro khóa nick vì dùng unofficial API - chỉ dùng nick phụ, giữ rate limit gửi tin.
- zca-js v2 bỏ sharp: gửi ảnh cần `imageMetadataGetter` - đã wire bằng package `image-size` trong `zalo-client.ts`.
